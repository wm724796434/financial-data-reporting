# 五篇大文章-3.10数字经济产业贷款发生额信息

> **来源**：[五篇大文章-3.10数字经济产业贷款发生额信息-原文.md](../参考资料/发文原文/五篇大文章-3.10数字经济产业贷款发生额信息-原文.md)

## 业务分类

| 属性 | 值 |
|------|-----|
| 接口表 |（无对应接口表）|
| 中文名 | 五篇大文章-3.10数字经济产业贷款发生额信息 |

---

# 第一部分：发文原文要求（人行规范层）

> **用于回答"这个表要求报送什么"、"新增业务应该报到哪个表"等问题"

## 1. 官方报送范围

本表报送报告期内数字经济产业贷款的发生额信息，按数字产业口径逐笔统计贷款发放金额。

## 2. 纳入与排除口径

| 方向 | 内容 |
|------|------|
| ✅ 纳入 | 报告期内发放的符合数字经济产业标识的贷款 |
| ❌ 排除 | 非数字经济产业标识业务 |

## 3. 关键业务要素

报送维度：客户、行业分类、发生金额、利率等。

## 4. 字段清单

完整字段清单详见原文文件。

---

# 第二部分：代码取数业务范围（实现层）

> **用于回答"这个表怎么取数"、"取了哪些业务"、"业务变更对金数有什么影响"等问题"

## 1. 数据生成程序

`bsp_sp_js_201_hdaszdkfs.prc`（加工层存储过程）- 生成数字经济产业贷款发生额明细数据

## 2. 监管集市表来源

| 表名 | 中文名 | 角色 |
|------|--------|------|
| `SMTMODS.L_ACCT_LOAN` | 贷款借据信息表 | **主表** |
| `SMTMODS.L_AGRE_LOAN_CONTRACT` | 贷款合同信息表 | **合同信息** |
| `SMTMODS.L_CUST_ALL` | 全量客户信息表 | 客户信息 |
| `SMTMODS.L_CUST_C` | 对公客户补充信息表 | **行业分类**—数字经济识别 |
| `SMTMODS.L_PUBL_RATE` | 汇率表 | 汇率折算 |

## 3. 输出接口表

`PBOCD_JS_201_HDASZDKFS` — 数字经济产业贷款发生额信息表

## 4. 业务筛选条件

从源码 `bsp_sp_js_201_hdaszdkfs.prc` 提取，结构与存量版（3.9）一致，分三个 UNION ALL 片段。**核心差异**：发生额版去掉了存量过滤条件（ACCT_STS、CANCEL_FLG、LOAN_ACCT_BAL>0、LOAN_STOCKEN_DATE IS NULL），增加了时间维度筛选和垫款剔除逻辑。

### 完整 WHERE 子句

```sql
-- ===== 第一段：一般贷款 =====
FROM SMTMODS.L_ACCT_LOAN A
INNER JOIN SMTMODS.L_CUST_C C
   ON A.CUST_ID = C.CUST_ID
  AND C.DATA_DATE = IS_DATE
  AND C.CUST_TYP <> '3'            -- 码表C0003：3=个体工商户，排除
INNER JOIN SMTMODS.L_AGRE_LOAN_CONTRACT D
   ON D.DATA_DATE = IS_DATE
  AND A.ACCT_NUM = D.CONTRACT_NUM
  AND SUBSTR(D.DIGITAL_ECONOMY_INDUSTRY, 1, 2) IN
      ('01', '02', '03', '04')     -- 数字经济产业识别（合同表字段）
WHERE A.FUND_USE_LOC_CD = 'I'      -- 码表A0006：I=境内（中国大陆），排除境外贷款
  AND A.ACCT_TYP NOT LIKE '0301%'  -- 码表A0004：0301=贴现，排除
  AND A.ACCT_TYP NOT LIKE '90%'    -- 码表A0004：90=委托贷款，排除
  AND A.DATA_DATE = IS_DATE
  AND (A.ACCT_TYP NOT LIKE '01%'   -- 码表A0004：01=个人贷款，排除
       OR A.ACCT_TYP LIKE '0102%') -- 但保留个人经营性贷款0102
  AND LENGTHB(A.ACCT_NUM) < 36     -- 账号长度校验
  AND TO_CHAR(A.DRAWDOWN_DT, 'YYYYMM') = SUBSTR(IS_DATE, 1, 6)  -- 发生额：放款日期在报告月份
  AND NOT EXISTS(                  -- 剔除当天发生当天收回的垫款
      SELECT 1 FROM SMTMODS.L_ACCT_LOAN A2
      WHERE A2.DATA_DATE = IS_DATE
        AND SUBSTR(A2.ITEM_CD, 1, 4) IN ('1306')       -- 垫款科目
        AND SUBSTR(TO_CHAR(A2.DRAWDOWN_DT, 'YYYYMMDD'), 1, 6) = SUBSTR(IS_DATE, 1, 6)
        AND A2.DRAWDOWN_DT = A2.FINISH_DT               -- 当天放当天结清
        AND A2.LOAN_ACCT_BAL = 0
        AND A.LOAN_NUM = A2.LOAN_NUM)

-- ===== 第二段：直贴业务 =====
FROM SMTMODS.L_ACCT_LOAN A
INNER JOIN SMTMODS.L_CUST_C C
   ON A.CUST_ID = C.CUST_ID
  AND C.DATA_DATE = IS_DATE
  AND C.CUST_TYP <> '3'            -- 排除个体工商户
WHERE FUND_USE_LOC_CD = 'I'        -- 境内贷款
  AND A.DATA_DATE = IS_DATE
  AND LENGTHB(A.ACCT_NUM) < 360
  AND SUBSTR(A.ITEM_CD, 1, 6) IN ('130101', '130104')  -- 直贴科目
  AND TO_CHAR(A.DRAWDOWN_DT, 'YYYYMM') = SUBSTR(IS_DATE, 1, 6)  -- 发生额时间维度
  AND SUBSTR(A.DIGITAL_ECONOMY_INDUSTRY, 1, 2) IN
      ('01', '02', '03', '04')     -- 数字经济产业识别（借据表字段）

-- ===== 第三段：直转业务 =====
FROM SMTMODS.L_ACCT_LOAN A              -- 原借据
INNER JOIN SMTMODS.L_ACCT_LOAN A1        -- 转贴入的直贴借据
   ON A1.DATA_DATE = IS_DATE
  AND A.ACCT_NUM || A.DRAFT_RNG = A1.ACCT_NUM || A1.DRAFT_RNG
  AND SUBSTR(A1.ITEM_CD, 1, 6) IN ('130101', '130104')
INNER JOIN SMTMODS.L_CUST_C C
   ON A.CUST_ID = C.CUST_ID
  AND C.DATA_DATE = IS_DATE
  AND C.CUST_TYP <> '3'            -- 排除个体工商户
WHERE A1.FUND_USE_LOC_CD = 'I'     -- 境内贷款
  AND A1.DATA_DATE = IS_DATE
  AND LENGTHB(A1.ACCT_NUM) < 360
  AND SUBSTR(A.ITEM_CD, 1, 6) IN ('130102', '130105')  -- 直转科目
  AND TO_CHAR(A.DRAWDOWN_DT, 'YYYYMM') = SUBSTR(IS_DATE, 1, 6)  -- 发生额时间维度
  AND SUBSTR(A1.DIGITAL_ECONOMY_INDUSTRY, 1, 2) IN
      ('01', '02', '03', '04')     -- 数字经济产业识别
```

### 4.1 与存量版（3.9）的关键差异

| 维度 | 存量版（3.9） | 发生额版（3.10） |
|------|-------------|----------------|
| **时间筛选** | 无（按数据日期取时点数据） | `TO_CHAR(DRAWDOWN_DT, 'YYYYMM') = 报告月` |
| **余额>0** | ✅ 要求 `LOAN_ACCT_BAL > 0` | ❌ 不要求 |
| **账户状态** | ✅ 要求 `ACCT_STS <> '3'`（排除结清） | ❌ 不要求 |
| **核销标志** | ✅ 要求 `CANCEL_FLG = 'N'` | ❌ 不要求 |
| **资产未转让** | ✅ 要求 `LOAN_STOCKEN_DATE IS NULL` | ❌ 不要求 |
| **垫款剔除** | ❌ 无 | ✅ 有 `NOT EXISTS` 子查询剔除当天发当天收回垫款 |

### 4.2 各条件码值含义

与存量版（3.9）一致的字段码值含义，见 [五篇大文章-3.9存量数字经济产业贷款信息.md](五篇大文章-3.9存量数字经济产业贷款信息.md) 的码值表格。

## 5. 涉及源码文件

| 角色 | 文件路径 |
|------|---------|
| 数据生成（主程序） | `源码/加工层存储/bsp_sp_js_201_hdaszdkfs.prc` |
| 数据生成（历史版本） | `源码/加工层存储/bsp_sp_js_201_hdaszdkfs_his.prc` |
