# 票据融资发生额信息_JS_205_PJRZFS — 字段取值

> 来源程序: `bsp_sp_js_205_pjrzfs.prc`

## 字段映射

| 序号 | 目标字段(alias) | 源表达式(expression) |
|------|----------------|---------------------|
| 1 | `(无)` | `T.USERNAME` |
| 2 | `(无)` | `T1.acct_Num` |
| 3 | `(无)` | `T1.draft_rng` |
| 4 | `(无)` | `T1.org_num` |
| 5 | `LOAN_ACCT_BAL_ZHITIE` | `T1.LOAN_ACCT_BAL AS LOAN_ACCT_BAL_ZHITIE` |
| 6 | `INT_ADJEST_AMT_ZHITIE` | `T1.INT_ADJEST_AMT AS INT_ADJEST_AMT_ZHITIE` |
| 7 | `LOAN_ACCT_BAL_ZHUANTIE` | `T2.LOAN_ACCT_BAL AS LOAN_ACCT_BAL_ZHUANTIE` |
| 8 | `INT_ADJEST_AMT_ZHUANTIE` | `T2.INT_ADJEST_AMT AS INT_ADJEST_AMT_ZHUANTIE` |
| 9 | `ID` | `1 AS ID` |
| 10 | `DATA_DATE` | `IS_DATE AS DATA_DATE` |
| 11 | `(无)` | `ORG_CODE` |
| 12 | `(无)` | `ORG_NUM` |
| 13 | `(无)` | `REG_REGION_CODE` |
| 14 | `(无)` | `DISCOUNT_TYPE` |
| 15 | `(无)` | `BILL_TYPE` |
| 16 | `(无)` | `BILL_MEDIUM` |
| 17 | `(无)` | `BILL_NUM` |
| 18 | `(无)` | `DISCOUNT_ID_TYPE` |
| 19 | `(无)` | `DISCOUNT_ID_NO` |
| 20 | `(无)` | `DISCOUNT_DEPT_TYPE` |
| 21 | `(无)` | `DISCOUNT_INDUSTRY_TYPE` |
| 22 | `(无)` | `DISCOUNT_REG_AREA_CODE` |
| 23 | `(无)` | `DISCOUNT_ENT_CON_ECO_ELEM` |
| 24 | `(无)` | `DISCOUNT_ENT_SCALE` |
| 25 | `(无)` | `ACCEPT_NAME` |
| 26 | `(无)` | `ACCEPT_ID_TYPE` |
| 27 | `(无)` | `ACCEPT_ID_NO` |
| 28 | `(无)` | `DRAWER_NAME` |
| 29 | `(无)` | `DRAWER_ID_TYPE` |
| 30 | `(无)` | `DRAWER_ID_NO` |
| 31 | `(无)` | `OPEN_DATE` |
| 32 | `(无)` | `BILL_DUE_DATE` |
| 33 | `(无)` | `TRANS_DATE` |
| 34 | `(无)` | `BILL_CURR_CODE` |
| 35 | `(无)` | `BILL_AMT` |
| 36 | `(无)` | `BILL_AMT_RMB` |
| 37 | `(无)` | `DISCOUNT_CURR_CODE` |
| 38 | `(无)` | `DISCOUNT_BAL` |
| 39 | `(无)` | `DISCOUNT_BAL_RMB` |
| 40 | `(无)` | `INT_RATE` |
| 41 | `(无)` | `REPORT_ID` |
| 42 | `CJRQ` | `IS_DATE AS CJRQ` |
| 43 | `(无)` | `BIZ_LINE_ID` |
| 44 | `(无)` | `VERIFY_STATUS` |
| 45 | `(无)` | `BSCJRQ` |
| 46 | `(无)` | `FRNBJGH` |
| 47 | `(无)` | `NBJGH` |
| 48 | `(无)` | `LOAN_CLASSIFY` |
| 49 | `(无)` | `LOAN_STATUS` |
| 50 | `(无)` | `COUNT(1)` |
| 51 | `DATA_DATE` | `VS_TEXT AS DATA_DATE` |
| 52 | `(无)` | `COALESCE(T3.ORG_NUM, A.ORG_NUM)` |
| 53 | `AFF_NAME` | `DRAWER_NAME AS AFF_NAME` |
| 54 | `AFF_ID_TYPE` | `DRAWER_ID_TYPE AS AFF_ID_TYPE` |
| 55 | `AFF_ID_NO` | `DRAWER_ID_NO AS AFF_ID_NO` |
| 56 | `DISCOUNT_AMT` | `CASE WHEN ID = '1' THEN DISCOUNT_BAL ELSE BILL_AMT END AS DISCOUNT_AMT` |
| 57 | `DISCOUNT_AMT_RMB` | `CASE WHEN ID = '1' THEN DISCOUNT_BAL_RMB ELSE BILL_AMT_RMB END AS DISCOUNT_AMT_RMB` |
| 58 | `TRANS_TYPE` | `CASE WHEN ID = '1' THEN CASE WHEN DISCOUNT_TYPE = '01' THEN 'A01' WHEN DISCOUNT_TYPE = '02' THEN 'A02' END ELSE 'B01' END AS TRANS_TYPE` |
| 59 | `SERIAL_NO` | `ID AS SERIAL_NO` |
| 60 | `REPORT_ID` | `CASE WHEN ID = '1' THEN CASE WHEN DISCOUNT_TYPE = '01' THEN '01MRKY' WHEN DISCOUNT_TYPE = '02' THEN '02MRKY' END ELSE CASE WHEN DISCOUNT_TYPE = '01' THEN '01MCKY' WHEN DISCOUNT_TYPE = '02' THEN '02MCK...` |
| 61 | `(无)` | `VS_TEXT` |
| 62 | `(无)` | `COALESCE(OB.ID_NO, OB.UP_ID_NO)` |
| 63 | `(无)` | `T.ORG_NUM` |
| 64 | `(无)` | `OB.REGION_CD` |
| 65 | `(无)` | `AFF_NAME` |
| 66 | `(无)` | `AFF_ID_TYPE` |
| 67 | `(无)` | `AFF_ID_NO` |
| 68 | `(无)` | `DISCOUNT_AMT` |
| 69 | `(无)` | `DISCOUNT_AMT_RMB` |
| 70 | `(无)` | `TRANS_TYPE` |
| 71 | `(无)` | `SERIAL_NO` |
| 72 | `(无)` | `IS_DATE` |
| 73 | `(无)` | `CASE WHEN T.ORG_NUM LIKE '51%' THEN '99' WHEN T.ORG_NUM LIKE '52%' THEN '99' WHEN T.ORG_NUM LIKE '53%' THEN '99' WHEN T.ORG_NUM LIKE '54%' THEN '99' WHEN T.ORG_NUM LIKE '55%' THEN '99' WHEN T.ORG_NUM ...` |
| 74 | `(无)` | `''` |
| 75 | `FRNBJGH` | `CASE WHEN T.ORG_NUM LIKE '51%' THEN '510000' WHEN T.ORG_NUM LIKE '52%' THEN '520000' WHEN T.ORG_NUM LIKE '53%' THEN '530000' WHEN T.ORG_NUM LIKE '54%' THEN '540000' WHEN T.ORG_NUM LIKE '55%' THEN '550...` |
| 76 | `(无)` | `TIME_TO_STR(STR_TO_DATE(IS_DATE, '%Y-%m-%d'), '%Y-%m-%d')` |
| 77 | `(无)` | `A.ORG_NUM` |
| 78 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN '01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN '02' ELSE NULL END` |
| 79 | `BILL_TYPE` | `CASE WHEN TRIM(B.BILL_TYPE) = '1' THEN '01' WHEN B.BILL_TYPE = '2' THEN '02' END AS BILL_TYPE` |
| 80 | `BILL_MEDIUM` | `CASE WHEN B.IS_P_BILL = 'Y' THEN '01' ELSE '02' END AS BILL_MEDIUM` |
| 81 | `(无)` | `a.acct_Num \|\| A.draft_rng` |
| 82 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND B.BILL_TYPE = '1' THEN 'A01' ELSE CASE WHEN NOT H.TYSHXYDM IS NULL THEN 'A01' WHEN LENGTH(H.ID_NO) = 18 THEN 'A01' WHEN NOT H.ORGANIZAT...` |
| 83 | `(无)` | `CASE WHEN (SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1') THEN COALESCE(CASE WHEN NOT H.TYSHXYDM IS NULL THEN H.TYSHXYDM WHEN LENGTH(H.ID_NO) = 18 THEN H.ID_NO WHEN NO...` |
| 84 | `(无)` | `CASE WHEN NOT T.CUST_ID IS NULL THEN 'C01' END` |
| 85 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1' THEN COALESCE(NVL2(D.CUST_ID, '100', SUBSTRB(TRIM(H.CORP_BUSINSESS_TYPE), 0, 3)), SUBSTRB(TRIM(H3.CORP_BUSINSES...` |
| 86 | `(无)` | `H.ORG_AREA` |
| 87 | `(无)` | `CD4.PBOCD_CODE` |
| 88 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1' THEN COALESCE(NVL2(H.CUST_ID, DECODE(H.CORP_SCALE, 'B', 'CS01', 'M', 'CS02', 'S', 'CS03', 'T', 'CS04', 'CS05'),...` |
| 89 | `(无)` | `COALESCE(CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') AND B.BILL_TYPE = '2' THEN B.AFF_NAME ELSE H2.CUST_NAM END, NVL2(FR.FINA_ORG_NAME_FR, FR.FINA_ORG_NAME_FR, B.PAY_BANK_NAME))` |
| 90 | `(无)` | `'A01'` |
| 91 | `(无)` | `COALESCE(CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') AND B.BILL_TYPE = '2' THEN H1.ID_NO ELSE H2.ID_NO END, NVL2(FR.FINA_ORG_NAME_FR, FR.LEGAL_TYSHXYDM_FR, B.PAY_BANK_CODE))` |
| 92 | `(无)` | `B.AFF_NAME` |
| 93 | `(无)` | `B.AFF_ID_NO` |
| 94 | `(无)` | `CAST(B.OPEN_DATE AS TEXT)` |
| 95 | `(无)` | `CAST(B.MATU_DATE AS TEXT)` |
| 96 | `TRANS_DATE` | `CASE WHEN A.LOAN_BUY_INT = 'N' THEN CAST(A.DRAWDOWN_DT AS TEXT) WHEN A.LOAN_BUY_INT = 'Y' THEN CAST(A.IN_DRAWDOWN_DT AS TEXT) ELSE CAST(A.DRAWDOWN_DT AS TEXT) END AS TRANS_DATE` |
| 97 | `(无)` | `TRIM(B.CURR_CD)` |
| 98 | `(无)` | `A.DRAWDOWN_AMT` |
| 99 | `(无)` | `A.DRAWDOWN_AMT * R.CCY_RATE` |
| 100 | `(无)` | `A.CURR_CD` |
| 101 | `(无)` | `A.DRAWDOWN_AMT - COALESCE(A.DISCOUNT_INTEREST, 0)` |
| 102 | `(无)` | `TRUNC((A.DRAWDOWN_AMT - COALESCE(A.DISCOUNT_INTEREST, 0)) * R.CCY_RATE, 2)` |
| 103 | `(无)` | `A.REAL_INT_RAT` |
| 104 | `TRANS_TYPE` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN 'A01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN 'A02' END AS TRANS_TYPE` |
| 105 | `SERIAL_NO` | `'1' AS SERIAL_NO` |
| 106 | `REPORT_ID` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN '01MRDY' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN '02MRDY' END \|\| SYS_GUID() AS REPORT_ID` |
| 107 | `BIZ_LINE_ID` | `CASE WHEN A.ORG_NUM LIKE '51%' THEN '99' WHEN A.ORG_NUM LIKE '52%' THEN '99' WHEN A.ORG_NUM LIKE '53%' THEN '99' WHEN A.ORG_NUM LIKE '54%' THEN '99' WHEN A.ORG_NUM LIKE '55%' THEN '99' WHEN A.ORG_NUM ...` |
| 108 | `VERIFY_STATUS` | `'' AS VERIFY_STATUS` |
| 109 | `BSCJRQ` | `IS_DATE AS BSCJRQ` |
| 110 | `FRNBJGH` | `CASE WHEN A.ORG_NUM LIKE '51%' THEN '510000' WHEN A.ORG_NUM LIKE '52%' THEN '520000' WHEN A.ORG_NUM LIKE '53%' THEN '530000' WHEN A.ORG_NUM LIKE '54%' THEN '540000' WHEN A.ORG_NUM LIKE '55%' THEN '550...` |
| 111 | `NBJGH` | `A.ORG_NUM AS NBJGH` |
| 112 | `(无)` | `CASE WHEN NOT T.acct_Num IS NULL THEN '01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN '01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN '02' ELSE NULL END` |
| 113 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND B.BILL_TYPE = '1' THEN COALESCE(CASE WHEN NOT H.TYSHXYDM IS NULL THEN H.TYSHXYDM WHEN LENGTH(H.ID_NO) = 18 THEN H.ID_NO WHEN NOT H.ORGA...` |
| 114 | `TRANS_DATE` | `CAST(A.MATURITY_DT AS TEXT) AS TRANS_DATE` |
| 115 | `DISCOUNT_AMT` | `A.DRAWDOWN_AMT AS DISCOUNT_AMT` |
| 116 | `DISCOUNT_AMT_RMB` | `A.DRAWDOWN_AMT * R.CCY_RATE AS DISCOUNT_AMT_RMB` |
| 117 | `TRANS_TYPE` | `'B01' AS TRANS_TYPE` |
| 118 | `SERIAL_NO` | `'0' AS SERIAL_NO` |
| 119 | `REPORT_ID` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN '01MCDY' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN '02MCDY' END \|\| SYS_GUID() AS REPORT_ID` |
| 120 | `(无)` | `LOAN_ACCT_BAL` |
| 121 | `(无)` | `ACCT_NUM` |
| 122 | `(无)` | `DRAFT_RNG` |
| 123 | `(无)` | `DATA_DATE` |
