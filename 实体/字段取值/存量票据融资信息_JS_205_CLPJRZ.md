# 存量票据融资信息_JS_205_CLPJRZ — 字段取值

> 来源程序: `bsp_sp_js_205_clpjrz.prc`

## 字段映射

| 序号 | 目标字段(alias) | 源表达式(expression) |
|------|----------------|---------------------|
| 1 | `(无)` | `T.USERNAME` |
| 2 | `(无)` | `COUNT(1)` |
| 3 | `(无)` | `IS_DATE` |
| 4 | `JRJGBM` | `'' AS JRJGBM` |
| 5 | `(无)` | `A.ORG_NUM` |
| 6 | `AREA_ID` | `'' AS AREA_ID` |
| 7 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN '01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN '02' ELSE NULL END` |
| 8 | `BILL_TYPE` | `CASE WHEN TRIM(B.BILL_TYPE) = '1' THEN '01' WHEN B.BILL_TYPE = '2' THEN '02' END AS BILL_TYPE` |
| 9 | `BILL_MEDIUM` | `CASE WHEN B.IS_P_BILL = 'Y' THEN '01' ELSE '02' END AS BILL_MEDIUM` |
| 10 | `(无)` | `a.acct_Num \|\| A.draft_rng` |
| 11 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1' THEN 'A01' ELSE CASE WHEN NOT H.TYSHXYDM IS NULL THEN 'A01' WHEN LENGTH(H.ID_NO) = 18 THEN 'A01' WHEN NOT H.ORG...` |
| 12 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1' THEN COALESCE(CASE WHEN NOT H.TYSHXYDM IS NULL THEN H.TYSHXYDM WHEN LENGTH(H.ID_NO) = 18 THEN H.ID_NO WHEN NOT ...` |
| 13 | `DISCOUNT_DEPT_TYPE` | `'C01' AS DISCOUNT_DEPT_TYPE` |
| 14 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1' THEN COALESCE(NVL2(D.CUST_ID, '100', SUBSTRB(TRIM(H.CORP_BUSINSESS_TYPE), 0, 3)), SUBSTRB(TRIM(H3.CORP_BUSINSES...` |
| 15 | `(无)` | `CASE WHEN COALESCE(D.REGION_CD, F.REGION_CD) = '999999' THEN H.ORG_AREA ELSE COALESCE(D.REGION_CD, F.REGION_CD) END` |
| 16 | `(无)` | `CD4.PBOCD_CODE` |
| 17 | `(无)` | `CASE WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') AND TRIM(B.BILL_TYPE) = '1' THEN COALESCE(NVL2(H.CUST_ID, DECODE(H.CORP_SCALE, 'B', 'CS01', 'M', 'CS02', 'S', 'CS03', 'T', 'CS04', 'CS05'),...` |
| 18 | `(无)` | `COALESCE(H1.CUST_NAM, NVL2(FR.FINA_ORG_NAME_FR, FR.FINA_ORG_NAME_FR, B.PAY_BANK_NAME))` |
| 19 | `(无)` | `'A01'` |
| 20 | `(无)` | `COALESCE(H1.ID_NO, NVL2(FR.FINA_ORG_NAME_FR, FR.LEGAL_TYSHXYDM_FR, B.PAY_BANK_CODE))` |
| 21 | `(无)` | `B.AFF_NAME` |
| 22 | `(无)` | `B.AFF_ID_NO` |
| 23 | `(无)` | `CAST(B.OPEN_DATE AS TEXT)` |
| 24 | `(无)` | `CAST(B.MATU_DATE AS TEXT)` |
| 25 | `TRANS_DATE` | `CASE WHEN A.LOAN_BUY_INT = 'N' THEN CAST(A.DRAWDOWN_DT AS TEXT) WHEN A.LOAN_BUY_INT = 'Y' THEN CAST(A.IN_DRAWDOWN_DT AS TEXT) ELSE CAST(A.DRAWDOWN_DT AS TEXT) END AS TRANS_DATE` |
| 26 | `(无)` | `TRIM(B.CURR_CD)` |
| 27 | `(无)` | `A.DRAWDOWN_AMT` |
| 28 | `(无)` | `A.DRAWDOWN_AMT * R.CCY_RATE` |
| 29 | `(无)` | `A.CURR_CD` |
| 30 | `(无)` | `A.DRAWDOWN_AMT - COALESCE(A.DISCOUNT_INTEREST, 0)` |
| 31 | `(无)` | `TRUNC((A.DRAWDOWN_AMT - COALESCE(A.DISCOUNT_INTEREST, 0)) * R.CCY_RATE, 2)` |
| 32 | `(无)` | `A.REAL_INT_RAT` |
| 33 | `(无)` | `SYS_GUID()` |
| 34 | `LOAN_CLASSIFY` | `CASE WHEN A.LOAN_GRADE_CD = '1' THEN 'FQ01' WHEN A.LOAN_GRADE_CD = '2' THEN 'FQ02' WHEN A.LOAN_GRADE_CD = '3' THEN 'FQ03' WHEN A.LOAN_GRADE_CD = '4' THEN 'FQ04' WHEN A.LOAN_GRADE_CD = '5' THEN 'FQ05' ...` |
| 35 | `(无)` | `CASE WHEN A.OD_FLG = 'Y' THEN 'LS03' WHEN A.EXTENDTERM_FLG = 'Y' THEN 'LS02' ELSE 'LS01' END` |
| 36 | `(无)` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN '01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104') THEN '01' WHEN SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105') THEN '02' ELSE NULL END` |
| 37 | `(无)` | `A.ACCT_NUM \|\| A.DRAFT_RNG` |
| 38 | `(无)` | `CASE WHEN NOT H.TYSHXYDM IS NULL THEN 'A01' WHEN LENGTH(H.ID_NO) = 18 THEN 'A01' WHEN NOT H.ORGANIZATIONCODE IS NULL THEN 'A02' ELSE COALESCE(CD1.PBOCD_CODE, CD2.PBOCD_CODE) END` |
| 39 | `(无)` | `CASE WHEN NOT H.TYSHXYDM IS NULL THEN H.TYSHXYDM WHEN LENGTH(H.ID_NO) = 18 THEN H.ID_NO WHEN NOT H.ORGANIZATIONCODE IS NULL THEN REPLACE(H.ORGANIZATIONCODE, '-', '') ELSE NVL2(D.CUST_ID, D.ID_NO, F.ID...` |
| 40 | `(无)` | `CASE WHEN NOT T.CUST_ID IS NULL THEN 'C01' ELSE NVL2(D.CUST_ID, 'D01', F.DEPT_TYPE) END` |
| 41 | `(无)` | `NVL2(D.CUST_ID, '100', SUBSTRB(TRIM(H.CORP_BUSINSESS_TYPE), 0, 3))` |
| 42 | `(无)` | `H.ORG_AREA` |
| 43 | `(无)` | `NVL2(H.CUST_ID, DECODE(H.CORP_SCALE, 'B', 'CS01', 'M', 'CS02', 'S', 'CS03', 'T', 'CS04', 'CS05'), '')` |
| 44 | `(无)` | `COALESCE(H2.CUST_NAM, NVL2(FR.FINA_ORG_NAME_FR, FR.FINA_ORG_NAME_FR, B.PAY_BANK_NAME))` |
| 45 | `(无)` | `COALESCE(H2.ID_NO, NVL2(FR.FINA_ORG_NAME_FR, FR.LEGAL_TYSHXYDM_FR, B.PAY_BANK_CODE))` |
| 46 | `TRANS_DATE` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN CASE WHEN T.LOAN_BUY_INT = 'N' THEN CAST(T.DRAWDOWN_DT AS TEXT) WHEN T.LOAN_BUY_INT = 'Y' THEN CAST(T.IN_DRAWDOWN_DT AS TEXT) ELSE CAST(T.DRAWDOWN_DT AS TEXT) END...` |
| 47 | `(无)` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN T.DRAWDOWN_AMT ELSE A.DRAWDOWN_AMT END` |
| 48 | `(无)` | `(CASE WHEN NOT T.ACCT_NUM IS NULL THEN T.DRAWDOWN_AMT ELSE A.DRAWDOWN_AMT END) * R.CCY_RATE` |
| 49 | `(无)` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN T.CURR_CD ELSE A.CURR_CD END` |
| 50 | `(无)` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN T.DRAWDOWN_AMT - COALESCE(T.DISCOUNT_INTEREST, 0) ELSE A.DRAWDOWN_AMT - COALESCE(A.DISCOUNT_INTEREST, 0) END` |
| 51 | `(无)` | `TRUNC((CASE WHEN NOT T.ACCT_NUM IS NULL THEN T.DRAWDOWN_AMT - COALESCE(T.DISCOUNT_INTEREST, 0) ELSE A.DRAWDOWN_AMT - COALESCE(A.DISCOUNT_INTEREST, 0) END) * R.CCY_RATE, 2)` |
| 52 | `(无)` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN T.REAL_INT_RAT ELSE A.REAL_INT_RAT END` |
| 53 | `LOAN_CLASSIFY` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN CASE WHEN T.LOAN_GRADE_CD = '1' THEN 'FQ01' WHEN T.LOAN_GRADE_CD = '2' THEN 'FQ02' WHEN T.LOAN_GRADE_CD = '3' THEN 'FQ03' WHEN T.LOAN_GRADE_CD = '4' THEN 'FQ04' W...` |
| 54 | `(无)` | `CASE WHEN NOT T.ACCT_NUM IS NULL THEN CASE WHEN T.OD_FLG = 'Y' THEN 'LS03' WHEN T.EXTENDTERM_FLG = 'Y' THEN 'LS02' ELSE 'LS01' END ELSE CASE WHEN A.OD_FLG = 'Y' THEN 'LS03' WHEN A.EXTENDTERM_FLG = 'Y'...` |
| 55 | `(无)` | `VS_TEXT` |
| 56 | `(无)` | `COALESCE(OB.ID_NO, OB.UP_ID_NO)` |
| 57 | `(无)` | `T.ORG_NUM` |
| 58 | `(无)` | `OB.REGION_CD` |
| 59 | `(无)` | `T.DISCOUNT_TYPE` |
| 60 | `(无)` | `CASE WHEN SUBSTRING(T.BILL_NUM, 1, 5) = '23132' THEN '02' ELSE T.BILL_TYPE END` |
| 61 | `(无)` | `T.BILL_MEDIUM` |
| 62 | `(无)` | `T.BILL_NUM` |
| 63 | `DISCOUNT_ID_TYPE` | `CASE WHEN NOT T.DISCOUNT_ID_NO IS NULL THEN 'A01' END AS DISCOUNT_ID_TYPE` |
| 64 | `(无)` | `T.DISCOUNT_ID_NO` |
| 65 | `(无)` | `CASE WHEN NOT T.DISCOUNT_ID_NO IS NULL THEN 'C01' END` |
| 66 | `(无)` | `T.DISCOUNT_INDUSTRY_TYPE` |
| 67 | `(无)` | `T.DISCOUNT_REG_AREA_CODE` |
| 68 | `(无)` | `COALESCE(T1.DISCOUNT_ENT_CON_ECO_ELEM, T.DISCOUNT_ENT_CON_ECO_ELEM)` |
| 69 | `(无)` | `T.DISCOUNT_ENT_SCALE` |
| 70 | `(无)` | `T.ACCEPT_NAME` |
| 71 | `(无)` | `COALESCE(T.ACCEPT_ID_TYPE, T1.ACCEPT_ID_TYPE)` |
| 72 | `(无)` | `COALESCE(T.ACCEPT_ID_NO, T1.ACCEPT_ID_NO)` |
| 73 | `(无)` | `T.DRAWER_NAME` |
| 74 | `(无)` | `T.DRAWER_ID_TYPE` |
| 75 | `(无)` | `T.DRAWER_ID_NO` |
| 76 | `(无)` | `T.OPEN_DATE` |
| 77 | `(无)` | `T.BILL_DUE_DATE` |
| 78 | `(无)` | `T.TRANS_DATE` |
| 79 | `(无)` | `T.BILL_CURR_CODE` |
| 80 | `(无)` | `T.BILL_AMT` |
| 81 | `(无)` | `T.BILL_AMT_RMB` |
| 82 | `(无)` | `T.DISCOUNT_CURR_CODE` |
| 83 | `(无)` | `T.DISCOUNT_BAL` |
| 84 | `(无)` | `T.DISCOUNT_BAL_RMB` |
| 85 | `(无)` | `T.INT_RATE` |
| 86 | `(无)` | `CASE WHEN T.ORG_NUM LIKE '51%' THEN '99' WHEN T.ORG_NUM LIKE '52%' THEN '99' WHEN T.ORG_NUM LIKE '53%' THEN '99' WHEN T.ORG_NUM LIKE '54%' THEN '99' WHEN T.ORG_NUM LIKE '55%' THEN '99' WHEN T.ORG_NUM ...` |
| 87 | `(无)` | `''` |
| 88 | `FRNBJGH` | `CASE WHEN T.ORG_NUM LIKE '51%' THEN '510000' WHEN T.ORG_NUM LIKE '52%' THEN '520000' WHEN T.ORG_NUM LIKE '53%' THEN '530000' WHEN T.ORG_NUM LIKE '54%' THEN '540000' WHEN T.ORG_NUM LIKE '55%' THEN '550...` |
| 89 | `NBJGH` | `T.ORG_NUM AS NBJGH` |
| 90 | `(无)` | `T.LOAN_CLASSIFY` |
| 91 | `(无)` | `T.LOAN_STATUS` |
| 92 | `(无)` | `T1.acct_Num` |
| 93 | `(无)` | `T1.draft_rng` |
| 94 | `LOAN_ACCT_BAL_ZHITIE` | `T1.LOAN_ACCT_BAL AS LOAN_ACCT_BAL_ZHITIE` |
| 95 | `INT_ADJEST_AMT_ZHITIE` | `T1.INT_ADJEST_AMT AS INT_ADJEST_AMT_ZHITIE` |
| 96 | `LOAN_ACCT_BAL_ZHUANTIE` | `T2.LOAN_ACCT_BAL AS LOAN_ACCT_BAL_ZHUANTIE` |
| 97 | `INT_ADJEST_AMT_ZHUANTIE` | `T2.INT_ADJEST_AMT AS INT_ADJEST_AMT_ZHUANTIE` |
| 98 | `(无)` | `*` |
