# 存款客户核对（JS_202_HDACLHLCK）

> **说明**：该报表无金融基础数据系统原文参考，数据来源于源码实现。

## 业务分类

| 属性 | 值 |
|------|-----|
| 接口表 | `JS_202_HDACLHLCK` |
| 中文名 | 存款客户核对（互联网存款存量核对） |
| 所属大类 | 存款类 |

---

# 第一部分：发文原文要求（人行规范层）

该报表在金融基础数据采集规范 V2.1 原文中未收录。此表为互联网存款业务客户信息交叉核对用表。

---

# 第二部分：代码取数业务范围（实现层）

> **用于回答"这个表实际怎么取数"、"取了哪些业务"、"业务变更对金数有什么影响"等问题**

## 1. 数据生成程序

`bsp_sp_js_202_hdaclhlck.prc`（加工层存储过程）

## 2. 监管集市表来源

| 表名 | 中文名 | 角色 |
|------|--------|------|
| `SMTMODS.L_ACCT_DEPOSIT` | 存款账户信息表 | **主表**—账户余额及介质类型 |
| `SMTMODS.L_ACCT_DEPOSIT_SUB` | 存款账户介质关系表 | **介质信息**—卡片类型及机构号 |
| `SMTMODS.L_CUST_P` | 对私客户补充信息表 | **客户信息**—客户名称 |

## 3. 输出接口表

`PBOCD_JS_202_HDACLHLCK` — 存量互联网存款信息核对表（15个字段）

## 4. 业务筛选条件

### 4.1 互联网存款渠道识别

通过借记卡产品类型（TYPE_ID）的前8位识别互联网存款渠道：

```sql
WHERE SUBSTR(T.TYPE_ID, 1, 8) IN ('62313113', '62313118')
```

| TYPE_ID前缀 | 渠道 | 渠道性质代码 |
|------------|------|-------------|
| 62313118 | 手机银行APP开立账户 | D01 |
| 62313113 | 市民卡APP开立账户 | D03 |

### 4.2 核心过滤条件

```sql
WHERE T.DATA_DATE = IS_DATE           -- 数据日期
  AND T.MEDIUM_STAT = 'A'             -- 介质状态：A=有效
  AND SUBSTR(T.TYPE_ID, 1, 8) IN ('62313113', '62313118')  -- 互联网存款渠道
  AND T1.ACCT_BALANCE <> 0            -- 余额不为0
```

## 5. 特殊处理规则

### 5.1 渠道性质与参与方证件代码映射

| 渠道 | 渠道性质 | 合作方证件代码 |
|------|---------|---------------|
| 62313118（手机银行APP） | D01 | 9122010170255776XN |
| 62313113（市民卡APP） | D03 | 91220102MA176CR29P |

### 5.2 机构代码映射

统一社会信用代码映射规则同其他报表（根据 ORG_NUM 前缀映射各分行/总行）。

### 5.3 数据关联

```sql
FROM SMTMODS.L_ACCT_DEPOSIT_SUB T                  -- 介质表（卡片类型、机构号）
INNER JOIN SMTMODS.L_ACCT_DEPOSIT T1                -- 账户表（余额）
LEFT JOIN SMTMODS.L_CUST_P P                        -- 客户名称
LEFT JOIN L_PUBL_ORG_BRA_TMP OB                     -- 机构代码映射
```

## 6. 涉及源码文件

| 角色 | 文件路径 |
|------|---------|
| 数据生成（主程序） | `源码/加工层存储/bsp_sp_js_202_hdaclhlck.prc` |
| 数据生成（解析版） | `源码解析/加工层存储/bsp_sp_js_202_hdaclhlck.prc` |
