# 小额企业信息_JS_102_XDQYXX — 取数范围

> 来源程序: `bsp_sp_js_102_xdqyxx.prc`

## 数据源表（FROM）

- (SELECT CUST_ID, ID_TYPE, ID_NO, CUST_NAM FROM SMTMODS.L_CUST_ALL AS B WHERE B.DATA_DATE = IS_DATE) AS B

## 关联条件（JOIN）

- INNER JOIN (SELECT CUST_ID, ID_TYPE, ID_NO, CUST_NAM FROM SMTMODS.L_CUST_ALL AS B WHERE B.DATA_DATE = IS_DATE) AS B ON A.CUST_ID = B.CUST_ID

## 筛选条件（WHERE）

1. `TABLE_NAME = 'PBOCD_JS_102_XDQYXX'`
2. `PARTITION_NAME = 'P' || VS_NMONTH`
3. `A.RN = 1`
