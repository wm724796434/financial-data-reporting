# 存量票据融资信息_JS_205_CLPJRZ — 取数范围

> 来源程序: `bsp_sp_js_205_clpjrz.prc`

## 数据源表（FROM）

- (SELECT * FROM (SELECT A.*, ROW_NUMBER() OVER (PARTITION BY TYSHXYDM ORDER BY FINA_ORG_NAME NULLS LAST) AS RN FROM SMTMODS.L_CUST_BILL_TY AS A WHERE DATA_DATE = IS_DATE AND LEGAL_FLAG = 'Y' AND NOT TYSHXYDM IS NULL AND TYSHXYDM <> '000000000000000000' AND FINA_ORG_NAME NOT LIKE '%存托%' AND FINA_ORG_NAME NOT LIKE '%资管%' AND FINA_ORG_NAME NOT LIKE '%禁用%') AS A WHERE A.RN = 1) AS B
- (SELECT * FROM (SELECT A.CUST_ID, A.FINA_ORG_CODE, A.FINA_ORG_NAME, B.FINA_ORG_NAME AS FINA_ORG_NAME_FR, B.LEGAL_TYSHXYDM AS LEGAL_TYSHXYDM_FR, ROW_NUMBER() OVER (PARTITION BY A.FINA_ORG_NAME, A.FINA_ORG_CODE ORDER BY A.FINA_ORG_NAME NULLS LAST) AS RN FROM (SELECT * FROM (SELECT CUST_ID, FINA_ORG_CODE, FINA_ORG_NAME, TYSHXYDM, LEGAL_TYSHXYDM, ROW_NUMBER() OVER (PARTITION BY FINA_ORG_NAME, FINA_ORG_CODE ORDER BY TYSHXYDM NULLS LAST) AS RN FROM SMTMODS.L_CUST_BILL_TY AS A WHERE A.DATA_DATE = IS_DATE) AS A WHERE A.RN = 1) AS A LEFT
- L_CODE_DICTIONARY AS CD1
- L_CODE_DICTIONARY AS CD2
- L_CODE_DICTIONARY AS CD4
- L_PUBL_ORG_BRA_TMP AS OB
- PBOCD_JS_205_CLPJRZ_SQ AS T1
- SMTMODS.L_ACCT_LOAN AS T
- SMTMODS.L_ACCT_LOAN AS T2
- SMTMODS.L_AGRE_BILL_INFO AS B
- SMTMODS.L_CUST_ALL AS F
- SMTMODS.L_CUST_BILL_TY AS TY
- SMTMODS.L_CUST_C AS H
- SMTMODS.L_CUST_C AS H1
- SMTMODS.L_CUST_C AS H2
- SMTMODS.L_CUST_C AS H3
- SMTMODS.L_CUST_P AS D
- SMTMODS.L_PUBL_RATE AS R

## 关联条件（JOIN）

- INNER JOIN SMTMODS.L_AGRE_BILL_INFO AS B ON A.ACCT_NUM = B.BILL_NUM AND B.DATA_DATE = IS_DATE
- INNER JOIN (SELECT * FROM (SELECT A.CUST_ID, A.FINA_ORG_CODE, A.FINA_ORG_NAME, B.FINA_ORG_NAME AS FINA_ORG_NAME_FR, B.LEGAL_TYSHXYDM AS LEGAL_TYSHXYDM_FR, ROW_NUMBER() OVER (PARTITION BY A.FINA_ORG_NAME, A.FINA_ORG_CODE ORDER BY A.FINA_ORG_NAME NULLS LAST) AS RN FROM (SELECT * FROM (SELECT CUST_ID, FINA_ORG_CODE, FINA_ORG_NAME, TYSHXYDM, LEGAL_TYSHXYDM, ROW_NUMBER() OVER (PARTITION BY FINA_ORG_NAME, FINA_ORG_CODE ORDER BY TYSHXYDM NULLS LAST) AS RN FROM SMTMODS.L_CUST_BILL_TY AS A WHERE A.DATA_DATE = IS_DATE) AS A WHERE A.RN = 1) AS A LEFT JOIN (SELECT * FROM (SELECT A.*, ROW_NUMBER() OVER (PARTITION BY TYSHXYDM ORDER BY FINA_ORG_NAME NULLS LAST) AS RN FROM SMTMODS.L_CUST_BILL_TY AS A WHERE DATA_DATE = IS_DATE AND LEGAL_FLAG = 'Y' AND NOT TYSHXYDM IS NULL AND TYSHXYDM <> '000000000000000000' AND FINA_ORG_NAME NOT LIKE '%存托%' AND FINA_ORG_NAME NOT LIKE '%资管%' AND FINA_ORG_NAME NOT LIKE '%禁用%') AS A WHERE A.RN = 1) AS B ON A.LEGAL_TYSHXYDM = B.TYSHXYDM) WHERE RN = 1) AS FR ON B.PAY_BANK_ID = FR.FINA_ORG_CODE
- INNER JOIN SMTMODS.L_CUST_P AS D ON A.CUST_ID = D.CUST_ID AND D.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_ALL AS F ON A.CUST_ID = F.CUST_ID AND F.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_C AS H ON A.CUST_ID = H.CUST_ID AND H.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_C AS H1 ON B.PAY_CUSID = H1.CUST_ID AND H1.DATA_DATE = IS_DATE
- INNER JOIN L_CODE_DICTIONARY AS CD1 ON D.id_type = CD1.L_CODE AND CD1.CODE_CLMN_NAME = 'ID_TYPE'
- INNER JOIN L_CODE_DICTIONARY AS CD2 ON F.ID_TYPE = CD2.L_CODE AND CD2.CODE_CLMN_NAME = 'BZR_ID_TYPE'
- INNER JOIN L_CODE_DICTIONARY AS CD4 ON H.CORP_HOLD_TYPE = CD4.L_CODE AND CD4.CODE_CLMN_NAME = 'CORP_HOLD_TYPE'
- INNER JOIN SMTMODS.L_PUBL_RATE AS R ON R.DATA_DATE = IS_DATE AND R.BASIC_CCY = A.CURR_CD AND R.FORWARD_CCY = 'CNY' AND R.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_BILL_TY AS TY ON TY.CUST_ID = A.CUST_ID AND TY.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_C AS H3 ON H3.ID_NO = TY.TYSHXYDM AND H3.CUSTSTATUS = '01' AND H3.DATA_DATE = IS_DATE
- INNER JOIN (SELECT * FROM (SELECT A.*, ROW_NUMBER() OVER (PARTITION BY TYSHXYDM ORDER BY FINA_ORG_NAME NULLS LAST) AS RN FROM SMTMODS.L_CUST_BILL_TY AS A WHERE DATA_DATE = IS_DATE AND LEGAL_FLAG = 'Y' AND NOT TYSHXYDM IS NULL AND TYSHXYDM <> '000000000000000000' AND FINA_ORG_NAME NOT LIKE '%存托%' AND FINA_ORG_NAME NOT LIKE '%资管%' AND FINA_ORG_NAME NOT LIKE '%禁用%') AS A WHERE A.RN = 1) AS B ON A.LEGAL_TYSHXYDM = B.TYSHXYDM
- INNER JOIN SMTMODS.L_ACCT_LOAN AS T ON T.DATA_DATE = IS_DATE AND a.acct_Num || A.draft_rng = T.acct_Num || T.draft_rng AND SUBSTRING(T.ITEM_CD, 1, 6) IN ('130101', '130104')
- INNER JOIN SMTMODS.L_CUST_P AS D ON T.CUST_ID = D.CUST_ID AND D.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_ALL AS F ON T.CUST_ID = F.CUST_ID AND F.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_C AS H ON T.CUST_ID = H.CUST_ID AND H.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_CUST_C AS H2 ON B.PAY_CUSID = H2.CUST_ID AND H2.DATA_DATE = IS_DATE
- INNER JOIN L_CODE_DICTIONARY AS CD1 ON D.ID_TYPE = CD1.L_CODE AND CD1.CODE_CLMN_NAME = 'ID_TYPE'
- INNER JOIN PBOCD_JS_205_CLPJRZ_SQ AS T1 ON T.BILL_NUM = T1.BILL_NUM AND T1.CJRQ = VS_LAST_TEXT
- INNER JOIN L_PUBL_ORG_BRA_TMP AS OB ON OB.ORG_NUM = TRIM(T.ORG_NUM) AND OB.DATA_DATE = IS_DATE
- INNER JOIN SMTMODS.L_ACCT_LOAN AS T2 ON T2.DATA_DATE = IS_DATE AND SUBSTRING(T2.ITEM_CD, 1, 6) IN ('130102', '130105') AND T1.acct_Num || T1.draft_rng = T2.acct_Num || T2.draft_rng

## 筛选条件（WHERE）

1. `TABLE_NAME = 'JS_205_CLPJRZ'`
2. `PARTITION_NAME = 'JS_205_CLPJRZ_' || IS_DATE`
3. `A.DATA_DATE = IS_DATE`
4. `COALESCE(A.LOAN_ACCT_BAL, 0) + COALESCE(A.INT_ADJEST_AMT, 0) > 0`
5. `SUBSTRING(A.ITEM_CD, 1, 6) IN ('130101', '130104')`
6. `A.ACCT_NUM IN (SELECT ACCT_NUM FROM Z_BILL_NUM WHERE CO_NUM = '1')`
7. `SUBSTRING(A.ITEM_CD, 1, 6) IN ('130102', '130105')`
8. `TRIM(T.DATA_DATE) = IS_DATE`
9. `T1.DATA_DATE = IS_DATE`
10. `SUBSTRING(T1.ITEM_CD, 1, 6) IN ('130101', '130104')`
11. `a.cjrq = IS_DATE`
