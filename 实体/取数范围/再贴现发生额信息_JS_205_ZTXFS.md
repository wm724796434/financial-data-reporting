# 再贴现发生额信息_JS_205_ZTXFS — 取数范围

> 来源程序: `bsp_sp_js_205_ztxfs.prc`

## 数据源表（FROM）

- L_PUBL_ORG_BRA_TMP AS OB

## 关联条件（JOIN）

- INNER JOIN L_PUBL_ORG_BRA_TMP AS OB ON OB.ORG_NUM = T.ORG_NUM AND OB.DATA_DATE = IS_DATE

## 筛选条件（WHERE）

1. `T.CJRQ = IS_DATE`
2. `NOT EXISTS(SELECT 1 FROM PBOCD_JS_205_CLZTX_SQ AS F WHERE F.CJRQ = VS_LAST_TEXT AND T.BILL_NUM = F.BILL_NUM)`
3. `CJRQ = IS_DATE`
