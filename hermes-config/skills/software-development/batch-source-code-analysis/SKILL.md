---
name: batch-source-code-analysis
description: 批量分析源码目录中的源文件，提取元数据并生成结构化实体文档。专为数据库存储过程/SQL/Shell脚本等 ETL 类代码设计，特别处理中文注释编码问题。
category: software-development
---

# Batch Source Code Analysis — 批量源码分析与实体文档生成

## 触发条件

遇到以下情况时加载本技能：
- 用户要求"解析"或"分析"某个目录下的源码文件
- 用户要求创建"实体"目录来存放源码分析结论
- 用户要求为一批 Oracle PL/SQL、SQL、Shell 脚本生成结构化文档
- 需要从一个代码目录批量提取每个文件的功能、依赖、输出表等元信息
- 用户要求修复大量实体文件的 Part2 占位符问题（40+文件规模）
- 需要批量从 .prc 源码头部的 SMTMODS 表引用和修改历史来充实实体文件
- 用户问"某个字段怎么取数"——需要为源码文件添加行内字段映射注释（参见 `references/字段映射行内注释规范.md`）
- 需要核实或修复字段映射关系（来源表字段 → 目标接口表字段）

## 工作流程

### 阶段一：项目解剖（探索结构）

1. **先建立全貌**：用 `search_files(target='files')` 获取所有文件清单，按目录分组
2. **读项目约定文件**：如果存在 `AGENTS.md` 或 `CLAUDE.md`，先读，了解项目对"实体"目录的定义
3. **抽样读典型文件**：从不同子目录各读 1-3 个文件的头部（前 60 行），了解文件格式、注释模式、编码

### 阶段二：建立表名映射（监管集市表关联时必需）

1. **解析表清单建立物理表名→中文名映射**：
   ```python
   import re, json
   with open('监管集市表清单.md', 'r', encoding='utf-8') as f:
       content = f.read()
   table_map = {}
   for line in content.split('\n'):
       m = re.match(r'\|\s*\d+\s*\|\s*监管集市\s*\|\s*(\w+)\s*\|\s*([^|]+)\s*\|', line)
       if m:
           table_map[m.group(1)] = m.group(2).strip()
   json.dump(table_map, open('/tmp/table_map.json', 'w', ensure_ascii=False))
   ```

2. **从源码中提取表引用**：
   ```bash
   iconv -f GBK -t UTF-8 file.prc | grep -oP 'SMTMODS\.\w+' | sort -u
   ```
   或统一提取所有 PBOCD 接口表：
   ```bash
   grep -oP 'PBOCD_\w+' file.prc | sort -u
   ```

### 阶段三：脚本批量处理（推荐方案，替换阶段三/四）

当所有文件的处理逻辑一致时，**优先编写一个 Python 脚本用 execute_code 批量处理**，而不是逐个文件 read_file。这比 delegate_task 子任务模式更快（单次上下文，无子任务开销）。

**脚本模板**在 `templates/batch-enrich.py`，包含完整流程：

```
读取源码 → 解析注释 → 提取表引用 → 查监管集市表中名 → 构建增强注释 → 写回源码 → 同步更新实体文档
```

脚本需要处理的核心逻辑：

1. **构建增强头部注释**（.prc 文件）：
   ```
   程序名 + 业务域 + 用途 + 输出接口表 + 参数 + 引用的SMTMODS表（含中文名） + 修改历史
   ```

2. **实体文档更新**（保留已有内容，新增依赖关系表格）：
   - 用正则提取并保留已有的 `功能概述`、`关键逻辑`、`修改历史` 段落
   - 替换/新增 `依赖关系（监管集市表）` 段落
   - 保留已有 `用途说明` 中的详细版本（优先于源码注释的简略版本）

   关键保留正则：
   ```python
   func_match = re.search(r'## 功能概述\n(.*?)(?=\n## )', existing, re.DOTALL)
   logic_match = re.search(r'## 关键逻辑\n(.*?)(?=\n## |$)', existing, re.DOTALL)
   history_match = re.search(r'## 修改历史\n(.*?)(?=\n## |$)', existing, re.DOTALL)
   ```

3. **.sql 文件处理差异**：
   - `.sql` 文件没有 `CREATE OR REPLACE PROCEDURE` 头部，也没有标准注释块
   - 处理方式：在文件顶部**新增**注释块（而不是替换），包含文件名、业务域、用途、引用的SMTMODS表
   - 实体文档同步方式与 .prc 相同，但元信息不含"存储过程名"而改为"文件类型: SQL脚本"

### 编码处理（关键！复用阶段二内容）

1. **读取**：Oracle PL/SQL 文件的中文注释通常为 GBK/GB18030 编码
   ```python
   def read_gbk(path):
       with open(path, "rb") as f:
           raw = f.read()
       try: return raw.decode("gbk")
       except: return raw.decode("utf-8", errors="replace")
   ```

2. **写回（重要pitfall！）**：
   - `write_file` 工具默认以 UTF-8 写入 → **会破坏 GBK 编码文件**！
   - 必须用二进制模式 + 指定编码写入：
     ```python
     def write_gbk(path, content):
         with open(path, "wb") as f:
             f.write(content.encode("gbk"))
     ```
   - 原始文件在 `金数源码/金数源码/` 中备份（原始编码不受影响），修改的是副本目录

3. **正则提取**：`用途[:：]\\s*(.*)`、`生成接口表\\s+(\\S+)`、`PROCEDURE\\s+(\\w+)`

4. **构建缩写映射字典**（从文件名推断）：
   ```python
   BIZ_MAP = {
       "101": "存款类 — 金融机构负债",
       "102": "客户信息类",
       "201": "贷款类",
       "202": "存款类",
       "203": "债券/投资类",
       "205": "票据类",
   }
   ```
   - 两种映射：裸缩写（CLDWDK→存量对公贷款信息）和 _HIS 后缀变体（自动去除 _HIS 后缀再查）
   - 数字编号前缀（如 201_）直接对应业务域

### 阶段三：域划分与并行调度

1. **按业务域/编号体系分组**（如数据库存储过程通常有编号：101=机构, 102=客户, 201=贷款, 202=存款, 203=债券, 205=表外等）
2. **每组分配一个 delegate_task 子任务**（最多 3 个并行）：
   - 每个子任务 context 中明确列出所有文件路径、已知用途、输出表
   - 设置 `toolsets=["file","terminal"]`（仅需读写文件和少量 shell）
3. **子任务内的工作流**：
   - 用 `read_file` 读每个文件的前 60-100 行（头部注释）
   - 对关键 SQL（INSERT/SELECT）跟进读取
   - 用 `write_file` 写入实体 .md 文件

### 阶段四：实体文件结构

每个实体文件遵循以下结构：

```markdown
# 文件名

## 基本元信息
- 存储过程名/脚本名:
- 业务域:
- 输出接口表:

## 用途说明
从注释提取的用途描述

## 功能概述
- 解决什么问题
- 在什么场景下触发（日终/月终/季度末）
- 数据流向

## 依赖关系
表格形式列出依赖的表/视图及其用途

## 关键逻辑
分条目列出核心加工规则

## 修改历史
需求编号、上线日期、修改原因
```

## 注意事项（踩坑记录）

### 编码问题
- Oracle PRC 文件的注释是 GBK 编码，用 UTF-8 直接 open 会乱码
- 在 execute_code 中使用 `for enc in ['gbk', 'gb18030', 'utf-8']` 逐一尝试解码
- read_file 工具能自动处理编码，优先使用
- **写回编码**：更新 .prc 文件头部注释时，必须用原编码（GBK）写回，否则中文会乱码。用 `iconv -f UTF-8 -t GBK` 转换后再写入

### 大文件处理
- 对 1000+ 行的 PRC 文件，只需读前 60-80 行和关键 SQL 段
- 实体文件是**总结性文档**，不是完整源码
- 标记"该文件共 X 行，详细 SQL 逻辑见源码注释"

### 子任务限制
- delegate_task 的 leaf agent 只有 terminal、read_file、write_file、search_files
- 无法使用 execute_code 做批量处理，必须逐文件 read_file
- 每个子任务的 context 要自包含——传入文件路径和已知元信息
- 子任务内 terminal 的 mkdir 可能因路径不存在失败，优先用 write_file（自动创建目录）

### 目录层级确认（高频踩坑）
- **创建分析/整理用的新目录前，必须用 `find` 确认目录结构全貌**
- 当项目中有同名嵌套目录时（如`金数源码/金数源码/`），极易混淆上下级关系
- "将A下的内容复制到B"——明确A是指子目录还是根目录。用绝对路径验证
- **先展示结构确认，再动手**：用 `clarify` 或书面展示让用户确认位置
- 不要因"看起来明显"就跳过确认步骤
- **"用户说A实际是指B"**：用户说"把实体文件名前面的日期去掉"，结果实际是指**原文文件名**（参考资料/金融基础数据系统/下的文件）。两个目录都有很多文件名且结构相似。如果用户描述的修改目标模糊（如"XX文件"），先用 `ls` 确认用户实际在指哪个目录，必要时用 clarify 确认后再动手

### 缩写翻译
- 银行/金融系统中文件名缩写密集（CL=存量 CLear, FS=发生额 FaSheng, HDA=核对 HeDui）
- 需要在主任务提前翻译好，作为 context 传给子任务
- _HIS 后缀的文件是历史镜像版本（取历史快照数据），逻辑与主版相同

### Markdown 链接编码问题（高频踩坑）
- 文件名包含括号 `()` 时（如 `单位存款连续性调整(村镇).md`），在 markdown 链接语法 `[text](path)` 中 **括号会被解析器截断**
- 修复方案：生成链接时对路径中的 `(` 和 `)` 进行 URL 编码：
  ```python
  path.replace("(", "%28").replace(")", "%29")
  ```
- 其他需要编码的特殊字符：空格（`%20`）、中文（自动编码）
- 验证手段：生成后遍历所有 `[text](path)` 链接，用 `os.path.exists()` 检查

### SMTMODS 表引用提取（大小写问题）
- SQL 代码中对表名的引用可能大小写混用（`smtmods.l_cust_all`、`SMTMODS.L_CUST_ALL`）
- 提取时必须使用 `re.IGNORECASE` 并统一转为大写，否则同表名会分裂为多个条目
  ```python
  # ❌ 错误：只能匹配大写
  for m in re.finditer(r'SMTMODS\\.(\\w+)', text):
  
  # ✅ 正确：不区分大小写，统一大写
  for m in re.finditer(r'SMTMODS\\.(\\w+)', text, re.IGNORECASE):
      tables.add(m.group(1).upper())
  ```

## 增强工作流：实体目录重组（从源码一对一 → 按报表聚合）

### 场景说明

项目初期，实体文件按源码文件**一对一**组织（`实体/加工层存储/xxx.md` 对应 `源码解析/加工层存储/xxx.prc`）。但当多个源码文件（加工层存储主程序 + 加工层特殊处理修正SQL + 应用层特殊处理汇总SQL）操作同一个**接口表（报表）**时，用户需要的是一个按报表聚合的实体文件，而不是分散的多个文件。

### 重组前确认

用 `clarify` 与用户确认聚合粒度，避免盲目重组：

| 粒度 | 说明 | 适用场景 |
|------|------|----------|
| **按接口表名** | 每个接口表（PBOCD_JS_xxx / JS_xxx）一个实体文件 | 大多数金融监管项目 |
| **按业务主题** | 贷款类、存款类、票据类等大类聚合 | 文档较少，按主题阅读 |
| **按报表输出途径** | 报送EAST的、报送PBOCD的 | 跨系统报送场景 |

### 重组流程

#### 第一步：建立接口表→源码文件映射

```python
# 扫描所有源码文件，提取 TRUNCATE/INSERT/MERGE INTO 的目标接口表
import re, json, os

DIRS = [
    {"key": "加工层存储", "src": "源码解析/加工层存储", "ext": ".prc"},
    {"key": "加工层特殊处理", "src": "源码解析/加工层特殊处理", "ext": ".sql"},
    {"key": "应用层特殊处理", "src": "源码解析/应用层特殊处理", "ext": ".sql"},
]

report_map = {}  # 接口表名 → {files: [...], all_smt_tables: set()}

for d in DIRS:
    for fname in os.listdir(d["src"]):
        if not fname.endswith(d["ext"]): continue
        content = open(os.path.join(d["src"], fname)).read()
        # 匹配三种输出表语法
        for m in re.finditer(
            r'(?:TRUNCATE\s+TABLE|INSERT\s+INTO|MERGE\s+INTO)\s+(?:\w+\.)?(PBOCD_[\w_]+|JS_[\w_]+|L_[\w]+)',
            content, re.I):
            tbl = m.group(1).upper()
            report_map.setdefault(tbl, {"files": [], "all_smt_tables": set()})
            report_map[tbl]["files"].append({"fname": fname, "dir": d["key"]})
```

**关键正则**：必须同时匹配 `TRUNCATE TABLE`、`INSERT INTO`、`MERGE INTO` 三种语法。只匹配前两种会遗漏 `MERGE INTO` 操作的表（加工层特殊处理和应用层特殊处理的常用语法）。

#### 第二步：过滤核心报表

去掉辅助表（`_TMP`、`_TEMP`、`_SQ`、`_MAPPING`、`_SPOP_BK`、日期后缀版本如 `_20251201`、日历后缀如 `_LOAN`）：

```python
def is_core_report(name):
    short = name.replace("PBOCD_DATACORE.", "").replace("PBOCD_", "").replace("JS_", "")
    if any(x in short for x in ["_TMP", "_TEMP", "_SQ", "_MAPPING", "_SPOP_BK"]): return False
    if re.search(r'_\d{8}$', short): return False  # 日期版本
    if short.endswith("_LOAN"): return False
    return True
```

#### 第三步：合并同源报表（别名归一化）

不同原始名前缀（`PBOCD_JS_201_CLDWDK` vs `JS_201_CLDWDK`）指向同一个核心报表：

```python
def normalize_name(n):
    n = n.replace("PBOCD_DATACORE.", "").replace("PBOCD_", "")
    if not n.startswith("JS_"): return f"JS_{n}"
    return n
```

用 `normalize_name` 作为字典 key 合并多个原始名的记录。

#### 第四步：构建实体文件内容（合并多源旧内容）

```python
def build_entity_content(primary_name, info):
    # 核心结构
    lines = []
    lines.append(f"# {cn_name}（{short_name}）")
    lines.append(f"> 接口表：`{primary_name}` · 业务域：{biz_tag}")
    
    # 报表说明 — 从主程序旧实体文件中提取用途说明
    # 涉及源码文件 — 按角色分类：
    #   ### 数据生成（加工层存储）
    #   ### 数据修正（加工层特殊处理）
    #   ### 数据引用（应用层特殊处理）
    # 每个文件列出文件名 + 角色 + 链接到源码解析
    # 引用的监管集市表 — 合并所有子文件的 SMTMODS 引用
    # 关键逻辑 — 从主程序旧实体文件中提取
    # 修改历史 — 合并所有子文件的历史记录（去重）
    # 双向链接 — 回首页 + 到源码解析主程序
```

**合并规则**：
- **报表说明/用途**：取主程序（加工层存储中生成该表的 .prc）的旧实体文件内容
- **关键逻辑**：同上，主程序内容为骨架
- **功能概述**：同上
- **修改历史**：合并所有子文件，去重
- **SMTMODS 表**：合并所有子文件的引用，去重

#### 第五步：删除旧实体文件 + 更新 index.md

```bash
rm -rf 实体/加工层存储 实体/加工层特殊处理 实体/应用层特殊处理
```

重新生成 index.md，所有 `实体/加工层存储/xxx.md` 链接改为 `实体/<中文名>_<接口表名>.md` 格式。

### 文件名安全化（高频踩坑）

实体文件名包含中文括号 `()` 时（如 `存量助学贷款(教育贴息)_xxx.md`），markdown 链接 `[text](path)` 中的括号会被解析器截断。**必须替换：**

```python
def safe_filename(s):
    """文件名安全化：括号替换为全角，斜杠替换为全角"""
    return s.replace("(", "（").replace(")", "）").replace("/", "／")
```

同理，链接路径生成时也要使用安全化后的中文名。

### JS_ 前缀一致性（高频踩坑）

接口表名标准化时，`short_name` 函数和实体文件名中的 `sn` 必须使用相同的策略——要么统一保留 `JS_` 前缀，要么统一去掉。不一致会导致 index.md 链接指向不存在的文件。

```python
# 必须一致！生成实体文件和 index.md 链接的 short_name 要用同一套逻辑
def short_name(t):
    n = t.replace("PBOCD_DATACORE.", "").replace("PBOCD_", "")
    if not n.startswith("JS_"): n = f"JS_{n}"
    return n  # 如 "JS_201_CLDWDK"
```

中文名映射查询链：
```
输入: "JS_201_CLDWDK"
→ 去掉 JS_ → "201_CLDWDK"
→ 去掉数字前缀 → "CLDWDK"  ← 查表
→ 返回 "存量单位贷款"
```

```python
def get_cn(short):
    candidates = [short]
    if short.startswith("JS_"): candidates.append(short[3:])
    for s in list(candidates):
        pure = re.sub(r'^\d+_', '', s)
        if pure != s: candidates.append(pure)
    for c in candidates:
        if c in CN_TABLE_MAP: return CN_TABLE_MAP[c]
    return short
```

### AGENTS.md 三层查询路由

重组完成后，**必须同步更新 AGENTS.md**，定义新的查询流程：

```markdown
第一层：index.md（一级路由）
  → 按业务域分组查报表 / 按源码文件反向索引查程序
  → 获取实体文件路径

第二层：实体文件（功能/概述层）
  → 角色分类：数据生成（加工层存储）/ 数据修正（加工层特殊处理）/ 数据引用（应用层特殊处理）
  → 获取报表说明、数据流转、引用的监管集市表

第三层：源码解析文件（字段级取数逻辑）
  → 用户追问字段细节时，查 SQL 代码中的具体字段映射
  → 结合参考资料/监管集市/下的表定义文档查字段中文名
```

删除 AGENTS.md 中所有引用旧实体路径（`实体/加工层存储/`）的示例，替换为新的路径格式。

## 增强工作流：基于原文文件的实体文档重建

### 场景说明

项目可能经历**三个阶段**的实体文档组织演变：

```
阶段一：一对一（源码→实体）
  实体/加工层存储/bsp_sp_js_201_cldwdk.md  ← 每个源码文件对应一个实体文件

阶段二：按接口表聚合（报表→源码）
  实体/存量单位贷款_JS_201_CLDWDK.md       ← 按接口表名聚合，汇总所有涉及该表的源码文件

阶段三：基于原文（监管规范→实体）
  实体/存量单位贷款信息_JS_201_CLDWDK.md   ← 以监管机构原文文件为基准重建
```

**阶段三的触发条件**：项目目录下出现 `参考资料/金融基础数据系统/` 或类似的监管规范原文目录，用户明确要求"以原文文件重新整理实体文件"。

### PDF补充：从规范文档提取缺失报表定义

#### 场景

当"不在原文中的报表"清单中的某些报表，在项目目录下存在《金融基础数据采集规范》等PDF文档时，可以从PDF中提取定义作为补充。

#### 工具与安装

```bash
sudo apt-get install -y poppler-utils
```

#### 提取策略

PDF通常为两栏布局（左栏接口表代码，右栏中文描述），纯文本提取有两种模式：

| 模式 | 特点 | 适用场景 |
|------|------|----------|
| `pdftotext -raw` | 按页流顺序提取，两栏文本交错 | 从混杂文本中提取每个报表的段落 |
| `pdftotext -layout` | 保持版面位置，两栏并行排列 | 查看页面结构，理解上下文 |

推荐用 `-raw` 模式 + 正则提取：

```bash
pdftotext -raw V2.1.pdf /tmp/pdf_raw.txt
```

#### 正则提取每个报表描述

```python
import re
with open("/tmp/pdf_raw.txt", "r", encoding="utf-8") as f:
    text = f.read()

# 模式1：JS_xxx独占一行（大多数情况）
pattern1 = re.compile(r'JS_xxx\n(.+?)(?=\nJS_\w+\n)', re.DOTALL)

# 模式2：JS_xxx 在同一行后跟中文（少数情况）
pattern2 = re.compile(r'JS_xxx\s+(.+?)(?=\nJS_\w+)', re.DOTALL)
```

#### 生成的补充原文文件结构

```markdown
# 中文名-原文（PDF补充）

## 来源信息
| 来源文档 | 金融基础数据采集规范 V2.1（PDF提取） |
| 表代码 | JS_xxx |
| 整理日期 | YYYY-MM-DD |

## 表级业务说明（报送范围）
（从PDF提取的描述文本，需清理两栏混入的杂音）

## 字段清单原文
> 字段清单见PDF原文（因表格提取限制，字段级定义请直接查阅PDF文档）
```

#### 注意事项

1. **文本杂音清理**：raw模式提取后，末尾可能混入右栏文本（如"存款同业存放"），需人工校对
2. **字段清单缺失**：PDF表格中的字段清单难以用pdftotext提取，在文件中指引到PDF原文
3. **文件名规范**：与原始原文文件格式保持一致 `JS_xxx_中文名-原文.md`（日期前缀已于2026-05-23统一移除，新文件不要加日期）
4. **来源标记**：实体文件中标注"📄 PDF补充"，区别于原始原文文件
5. **字段级完整提取**：当用户要求"通读PDF全部内容"时，需用章节定位+正则提取字段表格（见 `references/金融基础数据系统原文实体重建.md` 中"PDF补充：字段级表格提取（完整版）"章节）

#### 已知PDF补充成果参考

从"金融基础数据采集规范V2.1.pdf"（184页，44个接口表定义）中成功提取了4个缺失报表：
- JS_102_GRKHXX（个人客户基础信息）
- JS_102_TYKHXX（同业客户基础信息）
- JS_201_DBHTXX（担保合同信息）
- JS_202_CLGRCK（存量个人存款信息）

### 实体文件≠原文复制（用户明确纠正）

**核心原则**：实体文件是对原文的**总结**，加上从源码中提取的**关键逻辑**和**特殊处理规则**，而不是原文的复制品。

用户明确指出：*"实体文件不能跟原文一模一样，只是对原文的总结，以及源码内容的整理，外加源码的特殊处理逻辑"*

## 增强工作流：实体文件两分结构设计（2026-05-23 用户明确）

### 设计初衷

用户将项目知识域明确划分为两个独立维度，实体文件需对应承载：

| 问题域 | 查哪里 | 来源 |
|--------|--------|------|
| "**这个表要求报送什么**" / "**新增业务报哪个表**" | **第一部分：发文原文要求（人行规范层）** | 参考资料/金融基础数据系统/ 下的原文文件 |
| "**这个表怎么取数**" / "**取了哪些业务**" / "**业务变更影响**" | **第二部分：代码取数业务范围（实现层）** | 源码解析/ 下的源码文件和监管集市表 |

### 三阶段实体结构演变

```
阶段一：一对一（源码→实体）                 → 实体/加工层存储/bsp_sp_xxx.md
阶段二：按接口表聚合（报表→源码）             → 实体/存量单位贷款_JS_201_CLDWDK.md
阶段三：基于原文（两分结构）                  → 实体/存量单位贷款信息_JS_201_CLDWDK.md
    ├─ 第一部分：发文原文要求（人行规范层）
    └─ 第二部分：代码取数业务范围（实现层）
```

阶段三是阶段二的进化，不是替代——仍以接口表为聚合粒度，但在内容组织上拆为两个维度。

#### 最终实体文件结构

```markdown
# 报表中文名（JS_xxx）

> **来源**：[JS_xxx_中文名-原文.md](../参考资料/金融基础数据系统/JS_xxx_中文名-原文.md)

## 业务分类

| 属性 | 值 |
|------|-----|
| 接口表 | `JS_xxx` |
| 中文名 | xxx |
| 章节 | 3.x.x.x |
| 时间范围 | 报告期末存续数据 |
| 报送粒度 | 以贷款合同下单笔借据为单位逐笔报送 |

---

# 第一部分：发文原文要求（人行规范层）

> **用于回答"这个表要求报送什么"、"新增业务应该报到哪个表"等问题**

## 1. 官方报送范围
（从原文文件"官方报送范围"节提取，非整段复制，1-2句总结）

## 2. 纳入与排除口径
| 方向 | 内容 |
|------|------|
| ✅ 纳入 | 按官方报送范围采集规范定义的相关业务 |
| ❌ 排除 | 明确排除的业务类型 |

## 3. 关键业务要素（要求报送的内容维度）
（从原文文件"关键业务要素"节提取）

## 4. 字段清单
完整字段清单（共N个字段）及填报说明、码值表详见原文文件：
[原文文件名](../参考资料/金融基础数据系统/原文文件名)

---

# 第二部分：代码取数业务范围（实现层）

> **用于回答"这个表怎么取数"、"取了哪些业务"、"业务变更对金数有什么影响"等问题**

## 1. 数据生成程序
`bsp_sp_js_xxx.prc`（加工层存储过程）

## 2. 监管集市表来源
| 表名 | 中文名 | 角色 |
|------|--------|------|
| SMTMODS.L_xxx | xxx表 | 主表/左连 |

## 3. 业务筛选条件（代码实际取了哪些业务）
- **会计科目范围**：WHERE条件中的科目/账户类型过滤
- **客户类型范围**：JOIN条件中的客户类型过滤
- **排除条件**：核销、资产转让、余额为0等
- **产品类型映射**：ACCT_TYP/ITEM_CD → 产品类别码的映射关系

## 4. 特殊处理规则
（从加工层特殊处理SQL提取）

## 5. 历史变更记录
（从源码头部注释提取）

## 6. 涉及源码文件
- 数据生成：[prc文件路径]
- 数据修正：[sql文件路径]
- 数据引用：[sql文件路径]

---

> **使用说明**：
> - 问"**要求报送什么**"→ 查**第一部分（发文原文要求）**
> - 问"**怎么取数/取了哪些业务/业务变更影响**"→ 查**第二部分（代码取数业务范围）**

*← [返回首页索引](../首页/index.md) · 原文：[原文文件名](../参考资料/金融基础数据系统/原文文件名)*
```

### 关键原则

| 原则 | 说明 |
|------|------|
| **第一部分≠原文复制** | 第一部分是对原文的总结提炼（1-2句报送范围 + 纳入排除表格），不是原文整段粘贴。完整字段清单指引到原文文件路径，不复制字段表全文 |
| **第二部分≠字段映射** | 第二部分回答"取了哪些业务范围"（WHERE条件/科目/客户类型），不含字段级精确取数逻辑（如"到期日从哪个源字段取"）。字段级问题走第三层源码解析 |
| **五篇大文章特殊处理** | 无JS_xxx接口表代码的五篇大文章实体，标题不写空括号，来源直接引用原文文件名 |
| **双向指引** | 页脚保留回到首页index.md和原文文件的双向链接 |

### 批处理脚本模板（批量重构实体文件）

当需要将所有实体文件按新结构批量重构时，使用以下模式：

```python
import os, re

ENTITY_DIR = "实体"
ORIG_DIR = "参考资料/金融基础数据系统"

def parse_orig_content(orig_path):
    """从原文文件中提取结构化数据：报送范围、纳入排除口径、字段数等"""
    pass  # 详见 references/实体文件两分结构模板.md

def build_new_entity(name, iface, chapter, orig_basename, orig_data, old_content):
    """按两部分模板生成新实体文件内容"""
    pass  # 详见 references/实体文件两分结构模板.md

# 主流程
for ef in sorted(os.listdir(ENTITY_DIR)):
    if not ef.endswith('.md'): continue
    # 1. 读取旧实体文件 + 提取元信息（标题、接口表、章节、原文引用）
    # 2. 读取原文文件并解析结构
    # 3. 读取源码PRC头部提取修改历史和集市表引用
    # 4. 按模板构建新内容
    # 5. 写入实体文件
```

#### 已知边缘情况

1. **五篇大文章无接口表名**：旧标题格式为 `# 五篇大文章-3.X标题`（无括号接口表），正则 `^#\s*(.+?)(?:（(.+?)）)?` 匹配时会失败。修复方式：手动检查标题中是否含 `（）`，清除后重写来源行
2. **原文引用路径重复**：旧实体文件中 `来源` 行格式为 `[path](path)`，提取 `orig_file` 时需用 `os.path.basename()` 确保只取文件名，避免拼入 `../参考资料/` 前缀导致路径重复
3. **字段计数**：原文文件中字段表格的 `| 序号 |` 行可能有歧义。用 `re.findall(r'^\|\s*\d+\s*\|', content, re.MULTILINE)` 计数，适用于标准Markdown表格
4. **纳入排除口径解析**：原文文件中纳入排除可能写在同一段落而非表格中。提取后用关键词 `纳入`/`排除` 区分

### 关联的 AGENTS.md 更新

实体文件结构变更后，必须同步更新 AGENTS.md 中的第二层实体文件章节：
- 目录用途说明改为"两部分组织"
- 第二部分补充"查询场景路由"——什么场景查哪部分
- 删除旧结构的引用

#### 关键逻辑提取技术

从 `.prc` 源码文件中提取关键逻辑：

```python
def extract_key_logic_from_prc(prc_path):
    content = read_gbk(prc_path)
    logics = []
    
    # 1. 功能描述：从头部注释提取"用途"
    m = re.search(r'用途[：:]\s*(.*?)(?:\n)', content)
    if m: logics.append(f"**功能**：{m.group(1).strip()}")
    
    # 2. 关键变更：从修改历史提取修改原因
    changes = re.findall(r'修改原因[：:](.+?)(?:\n)', content)
    for c in changes[:3]:
        logics.append(f"**变更**：{c.strip()}")
    
    # 3. 映射规则：从CASE WHEN中提取关键映射
    case_map = re.findall(r'CASE\s+WHEN\s+([^)]+?)\s+THEN\s+["\']?(\w+)["\']?', content)
    seen = set()
    for cond, result in case_map:
        key = f"{cond[:30]} → {result}"
        if key not in seen and len(seen) < 5:
            seen.add(key)
            logics.append(f"**映射**：{cond[:40]}... → {result}")
    
    return logics[:8]
```

### 实体文件是总结不是复制品（用户核心纠正）

- ❌ **错误做法**：将原文的"数据范围/时间范围/报送粒度/纳入排除口径"整段复制到实体文件中
- ✅ **正确做法**：实体文件只包含三部分——原文精简总结（1-2句）+ 源码关键逻辑（CASE WHEN映射/功能描述/变更）+ 特殊处理规则（从SQL提取的原始注释），**字段清单仅指引到原文路径，不复制字段表全文**
- 用户原话：*"实体文件不能跟原文一模一样，只是对原文的总结，以及源码内容的整理，外加源码的特殊处理逻辑"*

### 特殊处理规则提取：跳过头部注释

SQL文件头部注释（如 `---分隔符` 之间的内容）是自动生成的，**不应包含**在实体文件的特殊处理规则中。只提取SQL文件中的**原始注释**：

```python
def extract_special_logic(sql_path):
    content = read_file(sql_path)
    
    lines = content.split('\n')
    in_header = False
    for line in lines:
        if line.strip().startswith('---') and len(line.strip()) > 10:
            in_header = not in_header  # 跳过分隔符之间的内容
            continue
        if in_header: continue
        # 只取第一条原始注释
        if line.strip().startswith('--') and '规则' in line:
            logics.append(f"**规则**：{line.strip().lstrip('-').strip()}")
            break
        elif line.strip().startswith('--') and len(line.strip()) > 10:
            logics.append(f"**处理**：{line.strip().lstrip('-').strip()}")
            break
```

#### 阶段三的关键差异

| 维度 | 阶段一/二 | 阶段三 |
|------|-----------|--------|
| 数据来源 | 源码文件注释 + SQL逻辑 | 监管机构原文文件（规范文档） |
| 内容重点 | 功能概述、关键逻辑、修改历史 | 原文总结 + 源码关键逻辑 + 特殊处理规则 |
| 报表说明 | 从源码注释提取 | 从原文提取第一句（精简总结） |
| 范围控制 | 按源码文件有无覆盖 | 按原文文件全量覆盖 |
| 缺失处理 | 无 | 需标记"有原文无实现"和"有实现无原文" |
| **关键逻辑** | 手动编写 | 从SQL代码自动提取（CASE WHEN/WHERE条件） |
| **特殊处理规则** | 无 | 从加工层特殊处理SQL提取原始注释 |

### 原文文件命名规范

原文文件采用**无日期前缀**的命名格式（日期前缀已于2026-05-23统一移除）：

```
JS_201_CLDWDK_存量单位贷款信息-原文.md
五篇大文章-3.5存量普惠贷款信息-原文.md
```

提取接口表名和中文名的正则（文件名已无日期前缀，无需strip）：

```python
def parse_original_filename(fname):
    name_part = fname.replace("-原文.md", "")
    m = re.search(r'(JS_[A-Z0-9_]+)', name_part)
    tbl = m.group(1).rstrip("_") if m else None
    # 五篇大文章需硬编码映射
    wupian_map = {"3.1存量科技贷款": "JS_201_HDACLKJDK", ...}
    if not tbl:
        for key, tbl_name in wupian_map.items():
            if key in name_part: tbl = tbl_name; break
    cn = name_part
    if tbl: cn = cn.replace(tbl, "").strip("_ -")
    return tbl, cn
```

### 实体文件重建流程

#### 第一步：对账（原文 vs 源码）

建立完整对照表，识别三类关系。

#### 第二步：从原文提取关键内容

正则提取标准化章节：报送范围、数据范围、时间范围、报送粒度、纳入排除口径。

#### 第三步：关联并分类源码文件

三段式角色分类：**数据生成**（加工层存储 .prc）、**数据修正**（加工层特殊处理 .sql）、**数据引用**（应用层特殊处理 .sql）。

#### 第四步：构建实体文件

结构（⚠️ 非原文复制，而是总结+源码逻辑）：

```
报表说明（精简总结，1-2句） → 非整段复制
业务分类（接口表代码、中文名、章节）
关键逻辑（从源码提取：功能描述 + CASE WHEN映射 + 关键变更）
特殊处理规则（从特殊处理SQL提取原始注释）
涉及源码文件（三段式：生成/修正/引用）
引用的监管集市表
字段清单（指向原文，不复制字段表）
```

⚠️ **不要**将原文的"数据范围/时间范围/报送粒度/纳入排除口径"整段复制到实体文件中——用户明确纠正过"实体文件不能跟原文一模一样"。

#### 第五步：更新路由索引和 AGENTS.md

1. 基于实体目录实际文件列表重写 index.md
2. AGENTS.md 实体文件说明改为"基于金融基础数据系统原文"
3. 补充"不在原文中的报表"表格

### 缺失处理规范

- **有原文无实现码**：实体文件中标注"（暂未找到对应源码）"
- **有实现无原文**：AGENTS.md 末尾建表格，查询时注明"无原文参考"

### 陷阱清单

1. **文件名安全化**：原文中文名中 `(教育贴息)` 的括号会被 markdown 解析器截断，必须替换为全角 `（）`
2. **五篇大文章无JS_表名**：需要硬编码章节号→接口表名的映射
3. **解析正则限定字符集**：`r'(JS_[A-Z0-9_]+)'` 而非 `\w+`，避免匹配到中文
4. **角色去重**：同一个文件可能出现在多个报表中，按 `(fname, dir)` 去重

## 增强工作流：监管集市表关联与双向同步

### 场景说明
当源码中引用 `SMTMODS.xxx` 等监管集市表，且项目目录下有对应的表定义文档时，需要对源码进行**交叉验证和同步更新**。

### 查询路径（固定顺序）
1. **先读表清单**：查找目录下的 `监管集市表清单.md`（索引文件），确认表是否存在
2. **从清单定位文件名**：清单列出序号、物理表名、中文名、字段数、文档链接
3. **读表定义文件**：提取关键元信息（中文名、主题、频率、字段数、关键字段说明）
4. **匹配到源码逻辑**：将表的中文名和字段说明映射到 SQL 的 INSERT/SELECT/JOIN 中

### 实体文件增强格式

在原有实体结构基础上，增加监管集市表关联信息：

```markdown
## 依赖关系（监管集市表）

| 表名 | 中文名 | 用途 |
|------|--------|------|
| SMTMODS.L_ACCT_LOAN | 贷款借据信息表 | 主数据源：提取贷款发放记录 |
| SMTMODS.L_CUST_C | 对公客户补充信息表 | 排除个体工商户 |
| ... | ... | ... |
```

关键原则：
- 每个引用的表**都必须配上中文名**（从监管集市表定义中查）
- 用途栏说明该表在存储过程中的具体作用（不写泛泛描述）
- 字段引用建议标注具体字段名（如 `DIGITAL_ECONOMY_INDUSTRY`）

### 双向同步流程

整理后的汇总信息需要同步两个位置：

```
源码解析/加工层存储/xxx.prc    ← 更新头部注释块（增强版）
实体/加工层存储/xxx.md          ← 更新实体文档（同步一致）
```

**更新 .prc 头部注释的规范格式**：

```sql
  ------------------------------------------------------------------------------------------------------
  -- 程序名
  -- BSP_SP_JS_201_XXX
  -- 业务域: xxx — xxx
  -- 用途: 生成接口表 PBOCD_JS_201_XXX，具体功能描述
  -- 输出接口表: PBOCD_JS_201_XXX
  -- 参数
  --    IS_DATE 输入变量，传入跑批日期
  --    OI_RETCODE 输出变量，标识执行过程是否异常
  -- 引用的监管集市表
  --    SMTMODS.L_xxx — xxx表（中文名 + 用途）
  --    ...
  -- 关键逻辑
  --    1. xxx
  --    2. xxx
  -- 修改历史
  --    2025-xx-xx（需求编号）原因，修改人，操作
  ------------------------------------------------------------------------------------------------------
```

**同步规则**：
1. 实体文档的内容必须与源码头部的注释一致（用途、逻辑、依赖关系）
2. 源码注释偏向"开发者视角"（参数、SQL逻辑细节）
3. 实体文档偏向"分析者视角"（业务背景、功能概述、依赖关系表格）
4. 差异检查清单：用途说明、依赖表、关键逻辑条目数

## 增强工作流：审计

审计流程已提取到独立技能 **[devops/project-audit](../skill/devops/project-audit)**，包含：
- 代码层数据源合规审计（SMTMODS 引用检查、裸表分类、违规程序定位）
- 文档层内容质量审计（Part1/Part2 占位符检查、引用覆盖度）
- 完整审计命令和审计报告模板

本技能中的审计相关章节已全部迁移到 **[project-audit](/.hermes/skills/project-audit/SKILL.md)**，此处不再重复。

## 增强工作流：项目级文档审计（AGENTS.md/README.md 版本核对）

### 场景说明

当用户要求"重新阅读整个项目"、"调整项目描述文件"、"更新 AGENTS.md/README.md 以反映实际状态"时，需要系统性地扫描项目，将文档中的数字和描述与实际文件结构进行一一核对。

### 审计流程

#### 第一步：全量目录扫描

用 `find` + `ls` + `wc -l` 获取每个目录的实际文件数：

```bash
# 顶层目录结构
ls -la

# 递归扫描并分组计数
find 实体/ -type f | wc -l
for dir in 源码/*/; do echo "$dir: $(find "$dir" -type f | wc -l)"; done
find 参考资料/ -type f | sort
```

#### 第二步：交叉验证 index.md 与实际目录

1. 读取 index.md 中声称的实体数量
2. 用 `ls 实体/*.md | wc -l` 获取实际实体文件数
3. 检查 index.md 是否遗漏新增实体或包含已删除的实体
4. 记录差异：index.md 有但实体目录没有 → 标记为"待删除"；实体目录有但 index.md 没有 → 标记为"待补充"

#### 第三步：生成项目总览表格

从实际扫描结果构建总览表格。**每个数字必须来自实际 `find`/`ls` 结果，不能从旧文档复制：**

```markdown
| 指标 | 数值 |
|------|------|
| 报表实体 | N 个（分组统计） |
| 源码文件（.prc） | N 个 |
| 特殊处理脚本（.sql） | N 个 |
| 监管集市表文档 | N 张 |
| 发文原文文件 | N 个 |
| 数据源审计报告 | N 份 |
```

#### 第四步：实体-源码交叉覆盖检查

除了核对数字，还需要从两个方向检查文档覆盖率的**缺口**：

##### 方向A：有实体无源码

从实体文件名提取接口表代码（`JS_xxx`），在源码目录中查找对应的 `.prc` 文件：

```bash
for entity in 实体/*.md; do
    base=$(basename "$entity")
    js=$(echo "$base" | grep -oP 'JS_\d+_\w+')
    if [ -n "$js" ]; then
        found=$(find 源码/ -name "*${js,,}*" 2>/dev/null)
        if [ -z "$found" ]; then
            echo "  有实体无源码: $base (接口表: $js)"
        fi
    fi
done
```

**注意**：实体文件名可能包含中文（如 `存量单位贷款信息_JS_201_CLDWDK.md`），`grep -oP` 提取 `JS_xxx` 部分是可靠的。但部分实体（如五篇大文章）可能没有 `JS_` 格式的接口表代码，需单独对照。

##### 方向B：有源码无实体

从 `.prc` 文件名提取接口表代码，在实体目录中查找对应文件：

```bash
for f in 源码/加工层存储/*.prc; do
    base=$(basename "$f")
    js=$(echo "$base" | grep -oP 'js_\d+_\w+')
    if [ -n "$js" ]; then
        found=$(find 实体/ -name "*${js^^}*" 2>/dev/null)
        if [ -z "$found" ]; then
            echo "  有源码无实体: $base (接口表: ${js^^})"
        fi
    fi
done
```

##### 结果分类

| 方向 | 含义 | 处理方式 |
|------|------|---------|
| A: 有实体无源码 | 该报表有文档但无对应代码 | 标记"源码缺失"；或确认代码在别的仓库 |
| B: 有源码无实体 | 程序在跑但没有文档 | 补建实体文件骨架（即使无原文也需有实现层文档） |
| ✅ 双向匹配 | 文档与代码覆盖完整 | 无需处理 |

#### 第五步：审计报告元验证

当项目中存在数据源审计报告或其他含数字的自述文档时，验证其声称的数值是否与实际一致：

```bash
# 源码文件数量验证
echo "实际源码文件数: $(find 源码/ -type f | wc -l)"
echo "实际实体文件数: $(ls 实体/*.md | wc -l)"

# 按类别验证审计报告中的细分数字
echo "加工层存储 .prc: $(ls 源码/加工层存储/*.prc 2>/dev/null | wc -l)"
echo "应用层特殊处理 .sql: $(ls 源码/应用层特殊处理/*.sql 2>/dev/null | wc -l)"
echo "加工层特殊处理 .sql: $(ls 源码/加工层特殊处理/*.sql 2>/dev/null | wc -l)"
```

逐项对比审计报告中的数字：
- 声称的总文件数 vs 实际
- 声称的各分类数量 vs 实际
- 声称的涵盖范围（目录路径）vs 实际存在的目录

**当发现差异时**：
1. 不要假设审计报告的数字是正确的
2. 以 `find`/`ls` 命令的实时输出为准
3. 更新审计报告中的数字和日期，标记旧数据已修正

#### 第六步：项目卫生检查

检查与代码质量无关但与项目运维相关的方面：

| 检查项 | 命令 | 关注点 |
|--------|------|--------|
| .gitignore完整性 | `cat .gitignore` | 是否缺少 `.env`、`*.csv`、`*.xlsx`、`*.dmp` 等敏感数据忽略规则 |
| Git配置状态 | `git config user.name` / `user.email` | 是否仍是临时配置（如 `wm@localhost`） |
| 根目录README | `ls -la README.md 2>/dev/null` | 是否缺少面向人类读者的项目简介 |
| 孤立文件 | 检查目录中是否有与项目主题无关的文件 | 如 `outfile.sh` 混在存储过程目录中 |
| 重复规则 | 检查 `.gitignore` 中是否出现重复条目 | 如 `*~` 出现两次 |

**建议修复方案**：
- `.gitignore` 补充 `*.env`、`*.csv`、`*.xlsx`、`*.xls`、`*.dmp` 并去重
- Git 配置设置真实的用户名和邮箱
- 如果存在身份不明的文件（如 `outfile.sh`），确认用途或在 AGENTS.md 中说明

#### 第七步：纠正文档中的过时数字

在 AGENTS.md 中全局搜索并更新以下常见过时模式：

- 报表实体数量（旧文档写49但实际可能是53）
- 源码文件数量（旧文档写67但实际可能是69）
- 目录说明是否匹配（新增了目录但描述未更新）
- 过时的路径引用（目录改名后未同步更新）

#### 第八步：提交文档更新

```bash
git add AGENTS.md
git commit -m "docs: 全面更新AGENTS.md，基于实际项目数据"
git log --oneline
```

### 行号前缀污染的诊断与修复

**现象**：文件内容每行开头带有 `行号|` 前缀（如 `1|# 标题`、`3|## 章节`），导致 markdown 标题标记无法正常渲染。

**诊断方法**：用 `head -5 <文件> | cat -A` 或 `xxd` 查看文件原始字节，确认行首是否有 `数字+|` 模式。

```bash
head -5 AGENTS.md | cat -A
# 输出示例: 1|# 标题$
# 如果看到行首有 "1|" 说明被行号污染
```

更精确的检测用 hex dump：
```bash
head -1 文件.md | xxd | head -3
# 如果开头字节是 "2020 2020 2031 7c"（空格+空格+空格+1+|）
# 说明存在行号前缀
```

**全面扫描所有 .md 分类**：行号前缀可能只影响部分分类，用分类扫描定位污染范围：

```bash
# 逐类检查受影响文件数量
for category in "实体" "首页" "参考资料/发文原文" "参考资料/监管集市" "参考资料/数据源审计"; do
    affected=$(grep -rlm1 '^[[:space:]]*[0-9]\+|#' "/path/to/$category"*.md 2>/dev/null | wc -l)
    total=$(ls "/path/to/$category"*.md 2>/dev/null | wc -l)
    echo "$category: $affected / $total 个文件受影响"
done
```

**批量修复命令**：

```bash
# 修复单个文件
sed -i 's/^[[:space:]]*[0-9]\+|//' 文件.md

# 批量修复整个分类
for f in 实体/*.md 首页/*.md 参考资料/数据源审计/*.md; do
    sed -i 's/^[[:space:]]*[0-9]\+|//' "$f"
done

# 验证修复
head -1 实体/*.md | cat -A | grep '|[0-9]|' || echo "全部已修复"
```

**成因**：常见于有人将终端工具（如 `cat -n`、`grep -n` 等）的带行号输出直接重定向保存为文件内容。

### 常见问题与修复

| 问题 | 诊断 | 修复 |
|------|------|------|
| 行号前缀污染 | `cat -A` 看到行首有 `数字+|`，或用 `xxd` 确认 | 分类扫描 → 批量 `sed -i`（见上方"全面扫描"） |
| index.md 声称数与实际实体数不符 | `ls 实体/*.md \\| wc -l` 对比文档 | 以实际文件为准更新文档数字 |
| 实体与源码覆盖不全 | 两个方向交叉检查（见第四步） | 补建实体或标记源码缺失 |
| 审计报告数字与实际情况不符 | 逐项核实审计报告中的分类计数 | 以实时 `find`/`ls` 输出为准更新报告 |
| 目录改名后路径引用过时 | 搜索旧目录名在所有文档中出现的位置 | 全局替换并验证 |
| Git 尚未初始化 | `git status` 报 `fatal: not a git repository` | `git init && git add . && git commit` |

## 增强工作流：批量填充 index.md 接口表字段

### 场景说明

当 index.md 中所有实体的"接口表"列都显示为"（无对应接口表）"时，实际每个实体文件内部有自己的接口表声明。需要从实体文件中提取并批量更新 index.md。

### 工作流

#### 第一步：从实体文件提取接口表映射

```bash
grep -rn '接口表 |' 实体/*.md | grep '`JS_' > /tmp/iface_map.txt
```

#### 第二步：用 Python 脚本（execute_code）批量替换

```python
import re
BASE = '/path/to/project'
path = f'{BASE}/首页/index.md'

with open(path, 'r', encoding='utf-8') as f:
    content = f.read()

# 对于"其他"分区中的非五篇大文章实体，从实体标题提取JS_xxx
lines = content.split('\n')
output = []
current_entity = None

for line in lines:
    m = re.match(r'^### (.+)$', line)
    if m:
        current_entity = m.group(1)
        output.append(line)
        continue
    
    # 跳过五篇大文章（他们没有独立接口表）
    if '| 接口表 | （无对应接口表） |' in line and current_entity and '五篇大文章' not in current_entity:
        js_m = re.search(r'JS_\d+_\w+', current_entity)
        if js_m:
            line = line.replace('（无对应接口表）', f'`{js_m.group(0)}`')
        output.append(line)
    else:
        output.append(line)

with open(path, 'w', encoding='utf-8', newline='\n') as f:
    f.write('\n'.join(output))
```

#### 第三步：验证

```bash
grep -n '接口表 |' 首页/index.md | head -15  # 确认五篇大文章仍为(无对应)
grep -n '接口表 |' 首页/index.md | tail -10  # 确认其他已填入JS_xxx
```

### 注意
- 五篇大文章报表通常没有独立接口表，必须保持为"（无对应）"
- 从实体标题提取 JS_xxx 是最可靠的方式（标题格式 `### 中文名_JS_xxx`）

## 增强工作流：为"有源码无原文"或"有原文有源码但占位符"的报表创建实体文件

### 场景说明

当审计发现某些报表存在以下两种情况之一时，需要补建或重建实体文件：

| 场景 | 表现 | 来源 |
|------|------|------|
| **有源码无原文** | 报表在 `.prc` 中有实现，但 `实体/` 目录中无文档 | AGENTS.md"不在原文中的报表"清单 |
| **有原文有源码但Part2写占位符** | 实体文件已存在，Part1 有原文摘要，但 Part2 全是"请查看源码" | 文档层审计发现 |

第二种情况最常见（40/58文件中占多数）：源码是完整的，原文也是完整的，但实体文件 Part2 只写了"请查看源码解析文件"而没有提取任何业务规则。

### ⚠️ 内容质量门槛（用户明确纠正）

**"有源码无原文"的实体文件不能只有表头骨架！** 第二部分必须包含从源码提取的实质性业务规则，包括：

| 内容项 | 来源 | 举例 |
|--------|------|------|
| 业务筛选条件（WHERE子句） | SQL 中的 WHERE/AND 条件 | `WHERE T.ITEM_CD IN ('130101', '130104')` |
| 代码映射表（DECODE/CASE WHEN） | CASE WHEN 逻辑 | 证件类型映射、渠道性质映射 |
| 渠道/产品识别规则 | TYPE_ID/ITEM_CD 过滤 | `SUBSTR(T.TYPE_ID, 1, 8) IN ('62313113', '62313118')` |
| 中间表说明 | TRUNCATE TABLE xxx_TMP | 每个临时表的用途 |
| 特殊处理规则 | 从源码提取的名词解释 | 去重逻辑、名称清洗等 |

**反例（被用户纠正）**：只有表格、没有实质规则的骨架文件（38~49行）
**正例（被用户认可）**：包含 SQL 过滤条件、编码映射表、中间表说明的充实文件（86~119行）

### 实体文件内容规范

```markdown
# 报表中文名（JS_xxx）

> **说明**：该报表无金融基础数据系统原文参考，数据来源于源码实现。

## 业务分类

| 属性 | 值 |
|------|-----|
| 接口表 | `JS_xxx` |
| 中文名 | xxx |
| 业务域 | xxx |

---

# 第一部分：发文原文要求（人行规范层）

该报表在金融基础数据采集规范 V2.1 原文中未收录。

---

# 第二部分：代码取数业务范围（实现层）

> **用于回答"这个表实际怎么取数"、"取了哪些业务"、"业务变更对金数有什么影响"等问题**

### 数据生成程序

| 程序名 | 角色 |
|--------|------|
| `xxx.prc` | 数据生成（主程序） |

### 监管集市表来源

| 表名 | 中文名 | 角色 |
|------|--------|------|
| `SMTMODS.L_xxx` | xxx表 | **主表**—用途说明 |

### 输出接口表

`JS_xxx` / `PBOCD_JS_xxx` — 中文描述

### 业务筛选条件（代码实际取了哪些业务）

#### 会计科目范围（核心过滤条件）

```sql
WHERE T.ITEM_CD IN ('xxx', 'yyy')
```

| 科目代码 | 说明 |
|----------|------|
| xxx | — |
| yyy | — |

#### 客户类型范围

```sql
WHERE P.OPERATE_CUST_TYPE IN ('A', 'B')
```

#### 渠道/产品识别

```sql
WHERE SUBSTR(T.TYPE_ID, 1, 8) IN ('xxx')
```

| 编码 | 渠道 | 说明 |
|------|------|------|
| xxx | 渠道A | 描述 |

### 特殊处理规则

#### 规则1：xxx

描述规则，附带 SQL 精要：

```sql
DECODE(条件, 'A', '映射值')
```

### 修改历史（如有）

| 编号 | 日期 | 修改人 | 原因 |
|------|------|--------|------|
| xxx | yyyy-mm-dd | 姓名 | xxx |

### 涉及源码文件

| 角色 | 文件路径 |
|------|---------|
| 数据生成（主程序） | `源码/xxx.prc` |
| 数据生成（解析版） | `源码解析/xxx.prc` |
```

### 🔴 批量充实 Part2 时的 `# 第二部分` 标题重复问题（高频踩坑）

当用脚本批量替换实体文件的 Part2 内容时，常见操作是：

```python
parts = content.split('# 第二部分')  # 分割旧文件
header = parts[0]
new_content = f'{header}# 第二部分{new_part2}'  # 拼接新内容
```

**问题**：旧文件的 Part2 标题是 `# 第二部分：代码取数业务范围（实现层）`，`split('# 第二部分')` 后 `parts[0]` 以 `\n# ` 结尾，拼接后产生 `# 第二部分` 与 `# 第二部分：` 连在一起的 `# 第二部分# 第二部分：` 重复标题。

**正确做法**：使用 `split('# 第二部分', 1)` 限制分割次数，并在 header 中去掉末尾可能残留的部分标题：

```python
parts = content.split('# 第二部分：代码取数业务范围', 1)
if len(parts) == 1:
    # 尝试短分割
    parts = content.split('# 第二部分', 1)
    header = parts[0].rstrip('# ')  # 去掉可能残留的标题前缀
else:
    header = parts[0]
new_content = f'{header}# 第二部分：代码取数业务范围（实现层）\n\n{new_part2}'

# 或在拼接前用 sed 修复
# sed -i 's/^# 第二部分# 第二部分/# 第二部分/' file.md
```

**验证命令**：
```bash
# 检查是否有重复标题
grep -c '# 第二部分# 第二部分' 实体/*.md
# 应为0
```

### 批量充实实体文件 Part2 的高效模式

当审计发现大量实体文件的 Part2 是占位符（"请查看源码"、"暂无对应"），需要从对应的 .prc 源码头部提取信息来充实，且文件数 > 10 时，使用以下模式避免工具调用次数超限：

**步骤一：用单个 terminal 调用读取全部源码头部**

```bash
# 一次性读取多个 .prc 文件的前25行，用 === 分隔
for f in prc_name_1 prc_name_2 prc_name_3; do
  echo "=== $f ==="
  head -25 "源码解析/加工层存储/${f}.prc" 2>/dev/null || echo "(NOT FOUND)"
done
```

**步骤二：构建 Python 数据字典**，从 terminal 输出中提取每组的程序名、业务域、用途、SMTMODS 表（含中文名）、修改历史。

**步骤三：用 execute_code 批量写回全部实体文件**

```python
data = {
    '个人客户基础信息_JS_102_GRKHXX': {
        'prc': 'bsp_sp_js_102_grkhxx.prc',
        'tables': [('L_CUST_ALL','全量客户信息表','主表'), ...],
        'history': ...
    },
}

for shortname, info in data.items():
    fpath = f'实体/{shortname}.md'
    content = open(fpath).read()
    header, _ = content.split('# 第二部分', 1)  # 保留 Part1
    new_part2 = build_part2(info)  # 构建新内容
    open(fpath, 'w').write(f'{header}# 第二部分：代码取数业务范围（实现层）\n\n{new_part2}')
```

**步骤四：验证**

```bash
grep -cE '请查看源码|暂无对应|当前实体暂无' 实体/*.md | grep -v ':0$'
# 应为空（无输出）
```

**注意**：注意 `# 第二部分` split 陷阱（见上方 pitfall）。

创建新实体文件时，**第一部分/第二部分的标题必须用 `#`（h1）**，与旧实体文件保持一致。旧文件使用：

```markdown
# 第一部分：发文原文要求（人行规范层）
# 第二部分：代码取数业务范围（实现层）
```

如果用 `##`（h2），会导致：
1. 视觉层级与旧实体不一致
2. 脚本统计第二部分内容量时漏计（`^# 第二部分` 匹配不到 `^## 第二部分`）
3. 导航结构不统一

### 信息提取来源

1. **程序名/用途/输出接口表**：从源码头部注释的 `-- 程序名`、`-- 用途`、`-- 输出接口表` 提取（前30行即可）
2. **监管集市表引用**：从源码头部注释的 `-- 引用的监管集市表` 区域提取
3. **修改历史**：从源码头部注释的 `-- 修改历史` 区域提取
4. **业务域**：从源码头部注释的 `-- 业务域` 提取
5. **WHERE 过滤条件**：从 SQL 语句的 WHERE 子句中提取，特别关注 ITEM_CD/ACCT_TYP/TYPE_ID 等科目/产品过滤条件
6. **DECODE/CASE WHEN 映射**：从 INSERT 语句的 SELECT 部分提取，这些构成业务规则的核心

### 🔴 关键验证：确认第二部分非占位符

实体文件创建或批量修改后，必须验证第二部分是否为**实质性内容**而非占位符：

```bash
# 验证1：无占位符残留
grep -c '请查看原文' 实体/*.md          # 应为0
grep -c '暂无对应' 实体/*.md            # 应为0
grep -c '暂无可参考' 实体/*.md          # 应为0
grep -c '查看源码解析' 实体/*.md        # 应为0

# 验证2：检查实际SQL条件存在
grep -c 'WHERE\|ITEM_CD\|TYPE_ID' 实体/xxx.md  # 应有实质性WHERE条件

# 验证3：行数检查（低于50行通常是骨架文件）
wc -l 实体/*.md | sort -n | head -10   # 检查最小文件
```

**致命陷阱：不要仅看行数判断内容质量。** 五篇大文章文件之前有 34 行 Part2 但全是"（请查看源码解析文件）"——行数多不等于内容实。必须实际检查文件中是否包含SQL过滤条件或编码映射、角色说明等实质性信息，而不仅仅是跳过"占位符文本"的文本数统计。

**五篇大文章类文件特别容易中招**，因为它们没有独立接口表，容易被写成空壳。具体检查方法：
1. 读取文件第 37-40 行（Part2 开头），看第一个小节是不是有实际内容
2. 检查是否有 SQL 片段（`WHERE`、`DECODE`、`CASE WHEN` 等关键词）
3. 检查监管集市表引用行是否包含角色说明（"**主表**"、"**客户信息**"等加粗角色标注）而非空洞的表名罗列

### 后续更新

1. 创建实体文件后，同步更新 `index.md` 追加索引条目
2. 在 AGENTS.md 的"不在原文中的源码报表"表格中补充
3. 如果报表在 PDF 规范文档中有定义，可以在实体文件中标注"📄 可从PDF补充"

## 增强工作流：提案-批量执行模式

### 场景说明

当用户请求"项目改进"或"看看有什么需要修的"时，建议采用**先清单提案 → 用户确认 → 批量执行**的三段式工作流。

### 工作流

#### 第一步：清单提案

收集所有可改进项，分级分类呈现：

```
可做的：
✅ 事项A（做法：xxx）→ 我能独立完成
✅ 事项B（做法：xxx）→ 我能独立完成
❌ 事项C（做法：xxx）→ 需要人工参与

你选：全做 / 选做哪些？
```

#### 第二步：批量执行

用户确认后，优先并行执行无依赖的任务：
1. 简单的独立改动（sed批量、创建文件）→ 同时开始
2. 需要数据收集的改动 → 先并行收集数据，再批量应用
3. 需要读取大量文件的改动 → 用 execute_code 脚本批量处理

#### 第三步：回环修正准备

**切勿假设第一批输出就是最终版本。** 用户可能会在验证后发现内容质量问题，需要第二轮修正。常见回环场景：

| 场景 | 第一批输出 | 用户反馈 | 需要修正 |
|------|-----------|---------|---------|
| 实体文件内容不足 | 仅表头+骨架（38~49行） | "必要内容缺失，全都在引用原文" | 从源码提取业务规则充实到86~119行；注意五篇大文章也可能是骨架文件 |
| 五篇大文章空壳文件 | 70行但内容全是占位符 | "全都是查看原文文件和源代码" | 必须逐文件检查Part2是否有SQL条件/编码映射等实质性内容，而非只看行数 |
| 标题层级不一致 | 新文件用 `##` 旧文件用 `#` | 格式问题 | 统一标题层级 |
| 格式污染 | 行号前缀未清理 | "格式问题" | 批量 sed 修复 |

**策略**：第一批先做无风险的基础改动（格式化、创建文件骨架等），留出第二轮修正的余量。

#### 第四步：验证 + 汇总

完成后输出**执行结果表**，与提案一一对应：

```
| 事项 | 状态 | 改动内容 |
|------|------|----------|
| ✅ A | 完成 | 改了什么 |
| ✅ B | 完成 | 改了什么 |
```

## 增强工作流：README.md 初始化

### 场景说明

当项目根目录缺少 README.md 时，创建一个简洁的项目简介。

### 模板

```markdown
# 项目名

一句话描述（如：金融基础数据监管报送系统的存储过程和 SQL 脚本库）。

## 目录结构

| 目录 | 说明 |
|------|------|
| `首页/` | N 个报表实体的完整索引（入口） |
| `实体/` | 每个报表的发文要求 + 代码取数实现说明 |
| `源码/` | 原始脚本备份 |
| `源码解析/` | 带增强注释的副本 |

## 快速开始

从 `首页/index.md` 开始，按业务域查找目标报表。

## 数据来源

基于 xxx 规范，结合 xxx 数据源实现。
```

### 原则
- 面向**人类浏览者**（GitHub 访客），非 Agent
- 与 AGENTS.md 互补——AGENTS.md 面向 AI Agent 定义查询协议，README.md 面向人提供快速概览

### 注意事项

1. **不要猜测文档中的数字来源**：如果文档写"49个报表"但实际扫出53个，以实际文件为准，不要试图解释为什么是49
2. **分步验证**：每改完一个部分就重新读一遍验证，避免批量修改引入新错误
3. **保留 Git 记录**：每次文档更新用语义化的 commit message 提交（`docs:` 前缀），方便回溯变更历史
4. **当用户说"格式问题"时**：不要自行猜测具体是哪种格式问题——可能是表格对齐、行号前缀、标题层级等各种可能性。先用 `cat -A` 或 `od -c` 确认文件字节状态，或者直接问用户看到什么现象
5. **Git 不会自动提交**：所有修改都只停留在工作区，必须手动执行 `git add` + `git commit` 才会记录。git 不会有任何"自动存档"行为

---

## 参考
- AGENTS.md 中的 Ingest 操作流程定义了"实体"文档规范
- AGENTS.md 禁止行为："不得将完整源码写入实体目录"
- `references/监管集市表关联工作流.md` — 监管集市表定义文件的查询步骤和依赖关系表格生成规范
- `references/金融系统缩写翻译.md` — 金融文件名缩写翻译字典（编号体系、业务类型缩写）
- `references/路由索引生成模式.md` — 双向交叉引用索引（by-report + by-file reverse index）的生成和工作原理
- `references/字段映射行内注释规范.md` — 为存储过程/SQL 源码的 SELECT 表达式添加行内字段映射注释（`-- → 目标字段  中文说明` 格式），解决"某个字段怎么取数"的三层查询瓶颈
- `references/跨引用文件维护工作流.md` — 批量重命名文件后更新所有 markdown 交叉引用的工作流（sed 全局替换 + 验证 + 技能库同步）
- `references/实体文件两分结构重构模板.md` — 实体文件两分结构设计的完整 Python 脚本模板和已知边缘情况处理（五篇大文章、路径重复、字段计数等）
- `references/金融基础数据系统原文实体重建.md` — 53个原文文件（含4个PDF补充）的完整对账表、五篇大文章→接口表名映射字典、接口表中文名映射字典、PDF提取方法、不在原文和PDF中的5个报表清单及三层查询路由
- `templates/batch-enrich.py` — 批量增强源码注释 + 同步实体文档的完整脚本模板。调用前需先建好 table_map.json，按项目修改 DIRS 配置和编码工具函数即可复用

## 扩展工作流：生成路由索引

### 场景说明
批量源码分析完成后，项目会拥有三类文件：
- `源码解析/` — 增强注释后的源码文件（.prc / .sql）
- `实体/` — 每个文件的总结文档（.md）
- `参考资料/监管集市/` — 监管集市表定义文档（.md）

**路由索引**的作用是将这三者串联起来，生成一个**双向查询入口**，让用户可以从"表名→程序"和"程序→表"两个方向自由导航。

### 三层查询架构

```
用户提问
  │
  ▼
第一层：路由索引（index.md）
  │  从表名查到程序列表
  │  从程序名查到实体文件链接
  ▼
第二层：实体文件（功能/概述）
  │  获取用途说明、功能概述、依赖关系
  ▼
第三层：源码文件（字段级）
    用户追问字段取数逻辑时 → 查SQL代码中的具体字段映射
```

### 路由索引文件结构

一个完整的路由索引包含两个部分：

#### 第一部分：按程序索引
每个程序一条记录，包含基本信息 + 引用的监管集市表清单：

```markdown
### BSP_SP_JS_201_HDASZDKFS

| 属性 | 值 |
|------|-----|
| 业务域 | 贷款类 |
| 实体文件 | [xx.md](实体/加工层存储/xx.md) |
| 源码文件 | [xx.prc](源码解析/加工层存储/xx.prc) |
| 用途 | 生成接口表 PBOCD_JS_201_HDASZDKFS ... |

**引用的监管集市表：**

| 物理表名 | 中文名 |
|----------|--------|
| `L_ACCT_LOAN` | 贷款借据信息表 |
| `L_CUST_C` | 对公客户补充信息表 |
```

#### 第二部分：按表反向索引
每个监管集市表列出被哪些程序引用：

```markdown
### L_ACCT_LOAN — 贷款借据信息表

被 39 个程序引用：

- [BSP_SP_JS_201_HDASZDKFS](实体/加工层存储/xxx.md)（加工层存储）
- [BSP_SP_JS_205_CLPJRZ](实体/加工层存储/xxx.md)（加工层存储）
```

### 生成脚本要点

1. **数据源**：本次从源码文件中提取的 `(程序名, [表名列表])` 映射
2. **表名统一**：用 `re.IGNORECASE` + `.upper()` 将所有表名转大写，避免大小写分裂
3. **链接编码**：路径中的括号 `()` 需转为 `%28`/`%29` 否则 markdown 链接断裂
4. **反向索引构建**：用字典 `{表名: [(程序名, 目录, 实体路径), ...]}` 汇总
5. **验证**：生成后遍历所有 `[text](path)` 链接，用 `os.path.exists()` 逐一确认
