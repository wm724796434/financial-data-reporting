     1|# 金融基础数据报送系统 — Agent 查询指南
     2|
     3|本仓库为金融基础数据报送系统项目，包含 EAST/PBOCD 等监管报送系统的存储过程和 SQL 脚本。
     4|
     5|## 目录结构
     6|
     7|| 目录 | 用途 |
     8||------|------|
     9|| `首页/` | **一级路由入口**：index.md — 49个报表实体的完整索引 |
    10|| `实体/` | **基于金融基础数据系统原文**的报表实体：每个实体文件按**两部分**组织——第一部分为发文原文要求（人行规范层），第二部分为代码取数业务范围（实现层） |
    11|| `源码解析/` | 带增强注释的源码副本（加工层存储 .prc / 加工层特殊处理 .sql / 应用层特殊处理 .sql） |
    12|| `参考资料/监管集市/` | 监管集市 191 个表的字段级定义文档 |
    13|| `参考资料/发文原文/` | 金融基础数据采集规范 V2.1 原文（49个）+ PDF补充（4个） |
    14|| `源码/` | 原始源码备份（不允许修改） |
    15|
    16|## 三层查询流程
    17|
    18|查询任何关于表、程序、取数逻辑的问题，必须遵循以下三层路由：
    19|
    20|### 第一层：index.md（一级路由）
    21|
    22|**作用**：定位目标报表/程序，获取实体文件路径。
    23|
    24|- **场景A**：用户问"XX报表是干什么的"
    25|  → 查 `首页/index.md`，按**业务域分组**（贷款类、存款类、客户信息类、票据类等）找到目标报表
    26|  → 点击实体文件链接进入第二层
    27|
    28|- **场景B**：用户问"XX程序的功能"
    29|  → 查 `首页/index.md` 末尾的"**按源码文件反向索引**"章节
    30|  → 找到该程序名，看它属于哪些报表，点击对应实体文件链接
    31|
    32|- **场景C**：用户问"某个表字段的取数逻辑"
    33|  → 先走第一层找到报表实体 → 再走第二层
    34|
    35|**禁止**：在未查 index.md 之前直接用 glob 搜索文件名或 grep 搜索内容。index.md 已收录全部 67 个源码文件到 52 个报表实体的映射关系，是第一查询入口。
    36|
    37|### 第二层：实体文件（两分结构）
    38|
    39|**作用**：获取该报表的**发文要求**和**代码实现**两个维度的信息。
    40|
    41|- 根据 index.md 中的链接读取 `实体/<报表名_接口表名>.md`
    42|- 实体文件按**两部分**组织：
    43|
    44|#### 第一部分：发文原文要求（人行规范层）
    45|
    46|> 回答"**这个表要求报送什么**"、"**新增业务应该报到哪个表**"等问题时查这里
    47|
    48|- **官方报送范围**：人行发文定义的表级业务说明
    49|- **纳入与排除口径**：发文明确包含和不包含的业务
    50|- **关键业务要素**：要求报送的内容维度（客户、合同、金额、利率等）
    51|- **字段清单**：发文定义的33个字段及填报规则摘要（完整信息指引到原文文件）
    52|- 来源：`参考资料/发文原文/` 下的原文文件
    53|
    54|#### 第二部分：代码取数业务范围（实现层）
    55|
    56|> 回答"**这个表怎么取数**"、"**取了哪些业务**"、"**业务变更对金数有什么影响**"等问题时查这里
    57|
    58|- **数据生成程序**：主存储过程
    59|- **监管集市表来源**：引用到的SMTMODS表及各自角色
    60|- **业务筛选条件**：代码中WHERE条件（科目范围、客户类型范围、排除条件、产品类型映射）
    61|- **特殊处理规则**：加工层特殊处理+应用层特殊处理SQL中的业务规则
    62|- **历史变更记录**：需求修改历史（编号、日期、修改人、修改内容）
    63|- **涉及源码文件**：按角色（数据生成/数据修正/数据引用）列出
    64|
    65|**限制**：实体文件回答报表的"概要性取数逻辑"（取了哪些业务范围和筛选条件），不包含字段级精确取数逻辑（如某个字段具体从哪个源字段映射）。字段级问题需进入第三层。
    66|
    67|### 第三层：源码解析文件（字段级取数逻辑）
    68|
    69|**作用**：当用户需要知道某个表字段的具体取数逻辑时，深入源码查找。
    70|
    71|- 读取 `源码解析/<子目录>/<文件名>`（.prc 或 .sql）
    72|- 源码文件头部注释包含：程序名、业务域、用途、输出接口表、引用的监管集市表列表、修改历史
    73|- 从 SQL 代码中查找具体的字段映射逻辑（如 `SELECT A.LOAN_NUM, A.ACCT_NUM...` 或 `WHERE A.FUND_USE_LOC_CD = 'I'`）
    74|- 结合 `参考资料/监管集市/` 下的表定义文档获取字段的中文名和码表说明
    75|
    76|## 查询示例
    77|
    78|> 用户："数字贷款发生额报表怎么取数的？"
    79|
    80|查询流程：
    81|1. **第一层**：读 `首页/index.md` → 在"五篇大文章"下找到"数字经济产业贷款发生额信息" → 获取实体文件 `实体/五篇大文章-3.10数字经济产业贷款发生额信息_JS_201_HDASZDKFS.md`
    82|2. **第二层**：读实体文件 → 获取报表说明、涉及的全部源码文件（bsp_sp_js_201_hdaszdkfs.prc生成主数据）、监管集市表引用
    83|3. **第三层**（用户追问字段细节）：读 `源码解析/加工层存储/bsp_sp_js_201_hdaszdkfs.prc` → SQL 中看到 `A.LOAN_NUM, A.ACCT_NUM, A.DRAWDOWN_AMT...` → 结合 `参考资料/监管集市/065_SMTMODS_L_ACCT_LOAN_贷款借据信息表.md` 查字段中文名
    84|
    85|## 查询规范
    86|
    87|1. **分层回答**：回答概括性问题时，先用第二层实体文件的内容输出概述，只有用户追问字段细节时才进入第三层源码文件
    88|2. **禁止深度扫描**：回答"XX报表是做什么的"这类问题，不得通读完整源码文件，应依赖 index.md 和实体文件
    89|3. **交叉验证**：如果用户发现实体文件内容与源码不一致，以源码为准，并更新实体文件
    90|4. **编码处理**：源码解析目录下的 .prc 文件为 UTF-8 编码，可直接读取；原始备份为 GBK 编码
    91|
    92|## 禁止行为
    93|
    94|- 禁止在未查 `首页/index.md` 的情况下直接用 glob/grep 搜索文件
    95|- 禁止回答概括性问题时通读完整源码解析文件（应先用实体文件回答概述）
    96|- 禁止回答时直接罗列函数/字段/文件路径作为第一层输出，必须先给出用户视角的描述
    97|
    98|---
    99|
   100|## 数据源审计
   101|
   102|### 审计原则
   103|
   104|原则上所有数据应从**监管集市（`SMTMODS` schema）** 取数。`SMTMODS` 是经过数据治理的统一数据源，直接引用上游业务系统或其他程序的输出接口表会导致数据链路不清晰、耦合度高。
   105|
   106|### 审计方法
   107|
   108|当需要审计数据源合规性时，执行以下步骤：
   109|
   110|1. **扫描全部源码文件**，提取所有 FROM/JOIN/INSERT/MERGE 语句中的表引用
   111|2. **按 schema 分类**：
   112|   - `SMTMODS.xxx` → ✅ 合规（监管集市）
   113|   - `CBRC_DATACORE.xxx` → ✅ 合规（银保监集市）
   114|   - `PBOCD_DATACORE.xxx` → ⚠️ 目标/中间表，需判断是否为数据源或自身输出
   115|   - 裸表名（无 schema 前缀） → ❓ 需逐个排查来源
   116|3. **标记违规程序**：完全不引用 `SMTMODS` 的程序
   117|4. **检查引用链**：使用接口表（`JS_xxx`）作为数据源的程序，应改为直接从 SMTMODS 取数
   118|
   119|### 审计命令参考
   120|
   121|```bash
   122|# 查找所有带 schema 前缀的表引用
   123|grep -rohP '\b[A-Z_]+\.\w+' --include="*.prc" --include="*.sql" . | sort -u
   124|
   125|# 提取 FROM/JOIN 中的 SMTMODS 表
   126|grep -rohP '(FROM|JOIN)\s+SMTMODS\.\w+' --include="*.prc" --include="*.sql" .
   127|
   128|# 提取 FROM/JOIN 中的裸表名（无 schema 前缀）
   129|grep -rohP '(FROM|JOIN)\s+(\w+)\s' --include="*.prc" --include="*.sql" .
   130|
   131|# 查找完全不引用 SMTMODS 的程序
   132|for f in $(find . -name "*.prc" -o -name "*.sql"); do
   133|    if [ "$(grep -c 'SMTMODS\.' "$f")" -eq 0 ]; then echo "$f"; fi
   134|done
   135|```
   136|
   137|### 分类标准
   138|
   139|| 标记 | 含义 | 处理方式 |
   140||------|------|---------|
   141|| ✅ 合规 | FROM/JOIN 直接引用 `SMTMODS.表名` | 通过 |
   142|| ⚠️ 间接引用 | 数据最终来自 SMTMODS，但经过中间表（如 `L_CUST_C_TMP`） | 建议优化为 SMTMODS 直取 |
   143|| ❓ 存疑 | 数据源不确定是否从 SMTMODS 派生 | 需人工核查 |
   144|| ❌ 违规 | 完全不引用 SMTMODS 或从非 SMTMODS 表直取 | 必须整改 |
   145|
   146|### 最新审计结果
   147|
   148|最新完整审计报告见：[参考资料/数据源审计/数据源审计报告.md](./参考资料/数据源审计/数据源审计报告.md)
   149|
   150|审计结论摘要：
   151|- **合规表**：SMTMODS 下 49 张表 ✅
   152|- **违规程序**：5 个程序完全不经过 SMTMODS（详见审计报告第三部分）
   153|- **间接引用表**：7 张表不在 SMTMODS schema 下但数据来自 SMTMODS
   154|- **配置/映射表**：5 张（可接受）
   155|
   156|---
   157|
   158|## 不在原文中的源码报表
   159|
   160|以下报表在源码中有实现，且金融基础数据系统原文和PDF发文中均未收录：
   161|
   162|| 接口表 | 说明 |
   163||--------|------|
   164|| JS_102_XDQYXX | 小额企业信息 |
   165|| JS_201_CLZXDK | 存量助学贷款 |
   166|| JS_201_HDACLHLDK | 存贷款客户核对 |
   167|| JS_202_WQYBJY | 完全一致交易 |
   168|| JS_202_FTYDWC_SMH | 非同业单位存贷（省农合） |
   169|
   170|查询时若涉及上述报表，请在回答中注明"该报表无金融基础数据系统原文参考"。
   171|
   172|---
   173|
   174|*最后更新：2026-05-20*
   175|